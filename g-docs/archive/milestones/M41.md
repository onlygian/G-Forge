# M41 — Release Machinery + README Currency

> 🗄 **ARCHIVED 2026-08-28 — DROPPED FROM v2.5, NOT PLANNABLE.** [ADR-012](../../decisions/012-g-forge-2.5-final-release-scope.md) amendment 4 makes v2.5 a minimal freeze and **drops M41 entirely**: v2.4.1 was hand-cut per G-RULES §D on 2026-08-24 and shipped, so release machinery built for a single remaining use is not worth building — M52 hand-cuts v2.5.0 the same way. This file moved out of `g-docs/milestones/` because that directory is `/g-plan`'s source of truth (`ROADMAP.md`); everything below is **historical record only**, including its `Status:` and `Depends on:` lines, and no part of it is scope any session should pick up. M41 is a **G-Proof candidate**, decided at R0 — see [`archive/roadmap-dropped-2026-08-28.md`](../roadmap-dropped-2026-08-28.md) for its roadmap entry and the drop rationale. Everything below this line is superseded by that stamp where the two disagree.

> ⚠ **ADR-010 CONTEXT STAMP (2026-07-26) — this milestone's own scope is intact; its surrounding narrative is not.** [ADR-010](../../decisions/010-full-rebuild-on-current-platform.md) freezes this repo after **v2.5** and forks the tree into a new repo transformed into G-Proof. Consequences for this file: **M41 cuts the v2.5 freeze release — the last release the 2.x line ever receives**, which raises its stakes rather than changing its tasks. *(Updated 2026-08-10, [ADR-012](../../decisions/012-g-forge-2.5-final-release-scope.md): v2.5 ships the full announced scope — M47, M48, M45, M38, M40, M43 all precede M41, which is sequenced LAST and cuts the release. "Only candidate" is retired.)* **Precondition for the release step: read `g-docs/communication-plan-2.5.md` before cutting v2.5.0** — it holds the approved release copy, placement rules, and the open §7 developer decisions that must be settled at publish time. **RETIRED wherever this file asserts it:** "the roadmap runs its whole natural life as G-Forge 2.x and restarts clean as G-Proof 1.0 at the end" · "M44 — the G-Proof 1.0 capstone, sequenced dead last" · the `v2.6.0 → v2.15.0` forward sequence. Under ADR-010, M44 is the **rebuild's release vehicle** (landing after the rebuild, from the forked repo), not this arc's capstone, and the 2.x sequence ends at v2.5. **Unchanged and still authoritative:** everything about `/g-changelog`, `/g-release`, the `/g-doctor` version-consistency check, README currency, and the rebrand-is-split-out-of-M41 fact itself. Whether M41 remains a prerequisite for M44 is an open gate call.

**Status:** 🗄 **Archived 2026-08-28 — dropped from v2.5** (ADR-012 amendment 4). Not plannable; G-Proof candidate, decided at R0. *(Was: ⬜ Not started, approved scope, ready for `/g-plan`.)*
**Version:** v2.5.0 (renumbered 2026-07-23 — M46 Update Integrity inserted ahead at v2.4.0; minor — new release commands + skills + a `/g-doctor` version-consistency check)
**Goal:** Make cutting a release a single gated step (`/g-changelog` + `/g-release`), and make README/CHANGELOG currency a structural property of every release rather than a memory-dependent chore.

> **RESTRUCTURED 2026-07-18 (developer decision):** the G-Proof rebrand, the full persuasion-ordered README restyle, and field communication were split OUT of this milestone into **`g-docs/milestones/M44.md` — the G-Proof 1.0 capstone, sequenced dead last**. The roadmap runs its whole natural life as G-Forge 2.x and restarts clean as G-Proof 1.0 at the end. This file previously carried the full rebrand scope; see M44.md for it. The README/CHANGELOG *currency convention* starts even before this milestone — at the v2.3.0 release (M-audit close ships the first status strip + CHANGELOG cut).

---

## Context

Six releases in ~2 weeks (2.0.0→2.2.1), each hand-editing the version in three places (`plugin.json`, `marketplace.json`, README counts) + cutting CHANGELOG `[Unreleased]`→dated + (always skipped) tagging. On 2026-07-12 a v2.2.2 bump was made mid-milestone and had to be reverted because nothing gated "is this a coherent, complete release?". On 2026-07-18 the developer flagged the GitHub README as visibly stale. Distribution is straight off `main` — the `plugin.json` version field IS the "latest available" signal every installed project sees, so a wrong bump ships immediately.

---

## Scope

### Wave 1 — Release Tooling (`/g-changelog`, `/g-release`)

**Tasks:**
1. Design `/g-changelog` skill:
   - Input: the **curated durable record** (milestone-ledger rows, review verdicts, plan done-conditions) as the primary source — **not raw `git log`** (ledger rows are already human-curated signal; commit messages are not). Git history is a cross-reference for confirmation only.
   - Output: draft `## [x.y.z] — date` block with categorized entries (Breaking Changes, Features, Fixes, Docs, Internal)
   - Option to prepend to CHANGELOG.md on approval
   - Gating: human reads + nods before any write
2. Implement `/g-changelog`:
   - Dispatch a `changelog-writer` agent (Haiku-tier) — ledger/review record as source, ADRs + git history for confirmation only, Keep-a-Changelog buckets inferred from row type
   - Return structured block for human review; write only on approval
3. Design `/g-release` skill:
   - Preconditions (hard gates, refuse — don't warn): active milestone ✅ closed; full suite green on a **real run** with pasted evidence (finding #20); gate self-hosted clean; no orphaned `[Unreleased]`; README status strip current (Wave 2)
   - Orchestration: confirm version (developer states major/minor/patch — the skill enforces consistency, never decides the bump) → run `/g-changelog` → one-shot version bump across **every** manifest → `[Unreleased]` → dated `## [x.y.z]` → annotated `v{x.y.z}` tag (closes the tagging gap the alveria adopter works around with pinned SHAs) → final `/g-review` on the release commit → push
   - Output: tag sha + release notes template
4. Implement `/g-release`.
5. Add the `/g-doctor` **version-consistency check**: manifests agree with each other; README counts match the `agents/` + `skills/` + `profiles/` inventory; README status-strip version matches `plugin.json`. Standing backstop against any hand bump.
6. Register both skills in `commands/g-forge.md`; add to README skills table.

**Depends on:** —

**Done condition:** `/g-changelog` drafts an entry from the ledger, human approves, it lands; `/g-release` refuses on a violated precondition (proven with a fixture or live partial-milestone attempt) and completes a full gated cut when preconditions hold; `/g-doctor` check flags a deliberately desynced manifest.

---

### Wave 2 — README Currency Machinery

**Tasks:**
7. `/g-release` precondition: the README **status strip** (version badge · "What's new" → CHANGELOG.md · "Where this is going" → g-docs/ROADMAP.md — first shipped at v2.3.0 by M-audit's release pass) is present and matches the release version being cut.
8. `/g-review` Step-6 close-out README-currency pass (optional, behavior-change-gated — the reusable per-milestone mechanism; drafted + nodded, never auto-applied).

**Depends on:** Wave 1 (the consistency check it extends)

**Done condition:** cutting a release with a stale strip is impossible via `/g-release`; the close-out pass exists and is documented in the skill.

---

## Explicitly Out of Scope

- **The G-Proof rename and everything branded** → M44 (capstone)
- **The full persuasion-ordered README restyle** (gate GIF, positioning narrative, before/after table, FAQ) → M44
- Publishing pipelines / CI, signing
- Changelog generation from raw commits (`git log` is never a source)
- Auto-deciding the semver bump
- Auto-applied README rewrites

---

## Premortem

| Risk | Scenario | Mitigation |
|------|----------|-----------|
| **Rubber stamp** (med) | Preconditions advisory → it just automates a bad bump | Milestone-closed + green-run + strip-current are hard gates; refuse, don't warn |
| **Version drift reappears** (low) | Someone hand-bumps one manifest | `/g-doctor` consistency check is the standing backstop |
| **Tag/manifest divergence** (low) | Tag cut separately from manifest write | Same run writes both; never a separate step |
| **Currency convention decays** (med) | Strip goes stale between releases | Strip check is a `/g-release` precondition, not a habit; doc gate covers CHANGELOG on every mixed/doc commit |

---

## Cross-cutting propagation (G-RULES §B)

The version number is a shared primitive read by `/g-update`, `workflow-checkpoint.sh` (daily update nudge), `/g-doctor`, and the manifests — `/g-release` must be the **single writer**, the `/g-doctor` check the **single verifier**. Run `/g-blast-radius` at Wave 1 close.

---

## Depends on

M-audit-2026-07 W1 (enforcement integrity) solid before shipping release machinery — **satisfied** (shipped v2.3.0). Sequenced **LAST** in the 2.5 build order per [ADR-012](../../decisions/012-g-forge-2.5-final-release-scope.md) (2026-08-10, amended 2026-08-20): M47, M48, M51 (absorbing M45), M50, M38, M40, M43, M49 all precede M41, which cuts v2.5.0. *(The earlier "immediately after M-audit, before M42" ordering is retired — M42 is fork-bound.)*

---

## Version-line note

| Milestone | Version |
|---|---|
| M-audit close | v2.3.0 (upgraded from v2.2.2 — new capability, not fixes) |
| M46 (Update Integrity, inserted 2026-07-23 ahead of this) | v2.4.0 |
| **M41 (this)** | **v2.5.0** |
| M45 → M43 | v2.6.0 → v2.15.0 (2.x line, see ROADMAP Version Plan) |
| M44 capstone | **G-Proof 1.0** (versioning restarts under the new name) |
