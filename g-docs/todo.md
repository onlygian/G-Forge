## Tasks
| # | Task | Notes |
|---|------|-------|
| 5 | **Audit Wave C — test teeth (M48 riders):** `hooks/pre-commit` ZERO execution coverage — new suite for the authoritative gate (audit-7 H1) · un-inert `test-workflow-checkpoint.sh:365` §13 (probe-proven green with guard deleted) · mode-detection assertion (probe P5) · falsifiability evidence 8/18 → convention per M48 | * fold into M48 /g-plan |
| 6 | **M51 rider (was M45 — folded 2026-08-20) — reviewer record-write contract:** the agents that instruct "write report to output_file" without holding a `Write` grant (derive the set at execution time by reading each `agents/*.md` body for a report-write instruction against its `tools:` line — frontmatter alone over-includes `pr-writer` — the hand-typed count drifted twice, 2026-08-12 and v2.4.1, so it is omitted per ADR-013); explains the two record-write stall incidents recorded against pre-fold M45. The open decision was scoped-Write-grants vs HQ-writes-records. **task-decomposer received its scoped grant early (2026-08-12, M47 C1 — its seam fix required it); the rest remain (doc-reviewer received its scoped grant in v2.4.1 — `CHANGELOG.md` `[2.4.1]`), wave-planner's /g-plan Step 3 seam and spec-writer's /g-refactor seam both **left** that population 2026-08-29 — M52 Tasks 7/8 closed them by the opposite mechanism (agent returns content, HQ writes the `output_file`; no grant). DECIDED by the 2026-08-20 directive: scoped Write grants (the doc-reviewer/task-decomposer pattern) — was M51 item 4; **not in 2.5**: M52's 2026-08-28 absorption narrowed item 4 to the two `output_file` dispatch sites + the README correction, so this row is now the sole owner of the remaining-agents half (`patterns-deferred.md` 2026-08-27 entry and `CHANGELOG.md` `[Unreleased]` both point here; retargeted 2026-08-29); this row tracks execution, not the decision** | * evidence: audit report 1 + M51 roadmap entry |
| 9 | **Attested-total confabulation, occurrence 4 of the claim-vs-data class.** `g-forge-dev` returned `GRAND TOTAL: 565 passed, 5 failed (18 suites)` against its own table of **17 rows summing to 537**; its `SUITES` block said `test-sentinel-read.sh: 16/1` while its own verbatim block said `16 passed, 0 failed`; its prose said "565 of 568" (565+5=570); and `test-pre-compact.sh` was **absent from both blocks**, its 22 assertions exactly the 564−542 gap. Re-run directly: pre-compact is **22/0 green**, so a passing suite was dropped and the hole filled with an invented number. Recorded here because the review record lives in gitignored `g-docs/agent-output/review/` and would otherwise be the only copy (same settlement as row 7). CLAUDE.md already documents three prior occurrences; HQ-sums-the-table catches it every time and prevents it never, which is the actual finding | carry to `/g-retro` then `/g-patterns` · feeds hallucination-rate, `g-docs/telemetry-metrics.md` §1 |
| 14 | **`/g-patterns` Step 14 anchor gap (doc-gate 2026-08-24 r1 W3):** `skills/g-patterns/SKILL.md:313`'s does-not-exist branch enumerates file-absent / `## [Unreleased]`-heading-absent / machine-generated — but not a present-but-**empty** `## [Unreleased]` (no `### Changed` beneath), which is the steady state after every release cut, so the gap-report branch never fires and Step 14 has no anchor to append under. Clarify that `### Changed` may be created under an existing `## [Unreleased]` while the `## [Unreleased]` heading itself never is (`:328`'s Rules line is ambiguous on this). Latent 50+ releases, not a regression. Shipped-source change → needs its own CHANGELOG entry; schedule into a milestone, never a release cut | doc-reviewer r1 record 2026-08-24, W3 |
| 11 | **Review-loop cost is now a THREE-PROJECT reproduced pattern, with a measured fix — feeds M51 (scope reviews — M45 folded into it 2026-08-20) and M48.** Evidence, independent sources: (a) **Alter-G**, `g-docs/retros/2026-08-17-alter-g-public-release.md:21` — a code gate given named files, a baseline declared already-reviewed and off-limits, exact commands, expected output and a tool-call target returned in **4 tool calls with zero findings**, while *the same work unscoped ran 20 minutes before being killed*; **reproduced twice**. Same retro `:27` — three string edits escalated to a full code gate when the real review surface was one comment line and two JSON name fields; `:9` — doc-gate findings took three rounds; `:28` — editing a file while a review agent reads it invalidates its verdict. (b) **G-Forge, this repo, 2026-08-16/17** — `/g-resume` Step 0 took **six review rounds** (findings 16 -> 19 -> 11 -> 10 -> 9 -> 2, every one genuine and closed), and one code-lead ran ~3h / ~130k tokens against a tree that had changed under it before being killed; a second was reviewing a stale snapshot after a concurrent session landed edits. (c) **keyline field report** `g-docs/field-reports/2026-08-10-keyline-francesco.md` — ~20 review dispatches for one milestone. **The pattern is not review depth, it is gate SCOPING**: an unscoped gate re-derives the whole surface every round, so cost scales with the repo instead of the diff, and the tree can move under it mid-read. Alter-G's scoped-gate result is the first measured evidence that the M51-inherited decision (review waves scoped by changes done + `/g-blast-radius`, whole-codebase audits proposed every 7-10 milestones) is worth what it costs. **Carry three concrete riders into M51's panel-wiring design (its scoping rule already mandates diff+blast-radius):** name the files and the already-reviewed baseline in the dispatch · match gate weight to the actual review surface, not to the changeset's file count · freeze the reviewed paths while a gate is in flight (or re-stamp and re-dispatch) | M51 panel-wiring input (was M45 design-ADR) + M48 rider · cross-project, cite all three sources |
| 15 | **Code-gate Minors carried from M52 Session B (r1 F1–F9 + r3 R3-2/R2-2/R2-3/R2-4 + r4 R4-1/R4-2/R4-4) and Session C (C-1…C-9 below), all Minor.** F6 closed 2026-08-30 (M52 F1); the rest are consistency/pinning items with no behaviour change. **F1 r1 Minor carried (code-lead `code-lead-2026-08-30-v25-f1-commit-gate-r1.md`, gitignored):** the raw-string boundary pad in `hooks/lib/commit-detect.sh` `_commit_detect_scan_segments` now pads `( ) { }` and backtick as well as `& \| ;`, so a *quoted pathspec* containing those characters (`-- "docs/report (final).md"`) is mutated inside its quotes before tokenizing — pre-existing class, safe direction (the mangled token classifies CODE → deny), but parens are common in real filenames. Design at task time: a quote-aware pad, pinned by a case. | * |
| 16 | **`commit-detect` false deny: a trailing command on the line after a gated `git commit` is walked as pathspecs (found 2026-08-30 on this repo).** Reproduced on the exact command text via `hooks/lib/commit-detect.sh` `extract_pathspecs` → `echo`, `rc=$?` → unmatched → CODE (`classify-changeset.sh:85`) → a doc-only staged set denied "Mixed commit missing code sign-off" (`check-commit.sh:253-254`). A newline is whitespace to the tokenizer (`commit-detect.sh:26-28`) and not a segment separator (`:247-268`); the `;` form is handled. Constraint on any fix: the whole-string newline flatten is deliberate (`:474-483`) and pinned green (`tests/test-commit-detect.sh:84`, `:89`, `:92`), so newline must not become a segment separator — design at task time, pinned by a new case. Fails closed (false deny, not a gate weakness); G-RULES §I (`rules/g-rules/I-project-tracking.md:57`) already forbids the trigger. Disposition: adopter-facing hook bug → M52 candidate under the ADR-012 amendment-4 filter, developer's call at Session C planning; otherwise G-Proof R0. Mechanism analysis, including the HEREDOC-p pin at `tests/test-commit-detect.sh:277-278`: `g-docs/agent-output/review/doc-reviewer-2026-08-30-todo-row16-r1.md` … `-r4.md` (gitignored). | |
| 18 | **F3 carries from the M52 F2 audit (`g-docs/audits/2026-08-30-fable-f2-survives-agents-record-seam.md`, "Out-of-slice observations"), evidence recorded there:** (1) `agents/review-orchestrator.md:11` "subagents cannot spawn other subagents" is false on the current platform — a dispatched `project-manager` holds an `Agent` tool (probe 2026-08-30; modernization GF-21 still `[VERIFY]`), and the README/§C claims that hang off it go with it; (2) `hooks/workflow-checkpoint.sh:389/:394` — presence-only `compact-state.md` test plus a whole-file `verify ADR` grep over `g-docs/ROADMAP.md` prints "a handed-off ADR needs verifying first" on every fresh session here (`ROADMAP.md:210` is body prose); (3) `skills/g-review/SKILL.md:85/:108/:208`, `agents/code-lead.md:38`, `agents/pr-writer.md:13`, `g-docs/agents.md:200` hardcode `main...HEAD` (twins of F2-6, found by the F2 carrier sweep); (4) `agents/feature-implementer.md`, `agents/pr-writer.md` have no `## Return format` block (twin of F2-2); (5) `code-reviewer`, `security-auditor`, `dependency-auditor` received the F2-3 memory rule but were not audited. | |

## Details

*(Task 2 — the whole-system audit — executed 2026-08-11; tasks 3–4 — Wave A+B remediation — executed later the same day (merge `29e4a98`, 4-round review). All three archived to `g-docs/todo-done.md` per §I. Rows 1 and 7 were archived 2026-08-17 (wiki refreshed; audit remainder fully disposed to M50/M48). Remaining remediation is task 5 above, plus the M50/M48 scope entries in `g-docs/ROADMAP.md`. Task numbers are stable references from the 2026-08-11 handoff; the #2–4 gaps are deliberate. Notes-column `*` on rows 5–6 marks an external pointer (rows 7 and 12 pointed externally too and are now archived — same settlement) to the named audit reports — this ledger currently carries no `### N` Details subsections; §I's asterisk convention is stretched knowingly until the next `/g-docs` settles it.)*

### 15 — Code-gate Minors carried from M52 Session B (waves 4–5)

Source records (gitignored): `g-docs/agent-output/review/code-lead-2026-08-29-v25-session-b-waves45-r1.md` (F1–F9) and `-r3.md` (R3-2, R2-2, R2-3, R2-4; R3-1 closed by doc fix round 4, 2026-08-30; code r4 record `-r4.md` adds R4-1/R4-2/R4-4 — R4-3 is a handoff-currency note consumed at pass close, not a task). All Minor, none blocking. **F6 closes before the branch's first push.**

| Ref | Site | Finding |
|---|---|---|
| F1 | `skills/g-refactor/SKILL.md:155` | Step 8 "fix" branch mints a non-discriminated output_file per blocking finding |
| F2 | `skills/g-skill-validate/SKILL.md:75-79` | "Do not hardcode a tool list" followed by a hardcoded copy of the three classes + unpinned line refs |
| F3 | `tests/test-readme-counts.sh:7-9` (+ `:87,:102,:105,:109,:113,:124,:127`) | Unpinned hand-typed counts and README line labels inside the count-pinning suite |
| F4 | `tests/test-observe.sh:19` · `tests/test-agent-lifecycle.sh:30` vs `tests/test-g-doctor-drift.sh:37-39` | Same claim class, three conventions in one wave |
| F5 | `tests/test-agent-lifecycle.sh:688-703` | 11.0 pins that the stub resolves, not that the jq tier produced the result |
| F7 | `hooks/observe.sh:192` | Second carrier of GNU-sed-only `\|` alternation (consistent with `check-commit.sh:83`; informational) |
| F8 | `skills/g-doctor/SKILL.md:190` · `skills/g-update/SKILL.md:251` | Zero-instance and no-frontmatter edges unspecified in architecture-skill drift/realign |
| F9 | `agents/spec-writer.md:57,60` vs `agents/wave-planner.md:64-73` | Two different answers to one problem in the same wave |
| R3-2 | `profiles/claude-plugin/agents/claude-plugin-architect.md:16` · `:35` | Unpinned "three tool classes" enumeration at edited sites (ADR-013 rule 2) |
| R2-2 | `agents/spec-writer.md:53` · `skills/g-refactor/SKILL.md:93` · `skills/g-skill-validate/SKILL.md:75` (added by fix round 4) | Unpinned hand-typed `architecture.md:6`/`:19` refs |
| R2-3 | `g-docs/patterns-deferred.md:3` | Enumeration incomplete by three |
| R2-4 | `profiles/claude-plugin/agents/claude-plugin-architect.md:35` · `skills/g-skill-validate/SKILL.md:83` (added by fix round 4; `review-orchestrator` disposition defined, class label unassigned) | "declared class" names a declaration with no source (no `class:` frontmatter field exists) |
| R4-1 | `profiles/claude-plugin/rules/architecture.md:6` · `skills/g-skill-validate/SKILL.md:81,:83` | `review-orchestrator` (dispatch grant only, empty file-access set) passes the fail rule but matches none of the three classes the text says agents come *in* — enumeration incomplete against it |
| R4-2 | `skills/g-skill-validate/SKILL.md:83` vs `profiles/claude-plugin/agents/claude-plugin-architect.md:35` | Opposite verdicts on `agents/project-manager.md:5`: validator clause (b) is unscoped and fires on PM's `Write, Edit`; architect clause 2 is reviewer-scoped and does not. Underneath: PM's `Write, Edit` grant vs the file-scope rule — since Wave F2 Task 29 (2026-08-30) that rule reads "Never touch implementation files" and names the PM's writable `g-docs/` files, so the live half is the validator-vs-architect clause mismatch, not the grant |
| R4-4 | `profiles/claude-plugin/rules/architecture.md:6,:19` | Dispatch is now explicitly off the class axis with no replacement check for a reviewer holding a dispatch grant to a writer child (transitive write path, G-RULES §C); no shipped agent triggers it |

**Session C set (2026-08-30, records gitignored: `code-lead-2026-08-30-v25-session-c-waves678-r1..r4.md`, `doc-reviewer-…-r1..r4.md`):**

| Ref | Site | Finding |
|---|---|---|
| C-1 | `hooks/check-commit.sh:276` | Pre-existing stderr advisory is discarded at exit 0 (Claude Code drops exit-0 stderr from a PreToolUse hook); the new REFERENCE advisory went to stdout for that reason. Move or drop the older one. |
| C-2 | `hooks/lib/classify-changeset.sh:129` | "genuinely inert formats" overstates `.html`/`.svg`/`.json` (script-capable); wording, not polarity. |
| C-3 | `hooks/check-commit.sh:209` | `-a` detection regex misses attached-value short clusters (`-am"msg"`). Pre-existing, backstopped by the native hook; execution verification owed. |
| C-4 | `hooks/lib/classify-changeset.sh:181` | Backslash pathspec (`skills\g-review\SKILL.md`) classifies DOC not CODE. Pre-existing, backstopped; severity conditional on Git-for-Windows normalisation, unverified. |
| C-5 | `g-docs/ROADMAP.md:406` | M40 risk register still lists the dropped doctor half as an in-scope mitigation. |
| C-6 | `tests/test-classify-changeset.sh:340` + header `:17,:22,:25` | Unpinned enumerations minted at edited sites ("all five classify as CODE"; per-round "Total assertions" ladder). ADR-013 rule 2: omit or pin. |
| C-7 | `CHANGELOG.md:17` | Probe-evidence pointer names "the review record", which is gitignored; name a committed carrier or drop the pointer (doc r3/r4 W1). |
| C-8 | `g-docs/todo.md` row 16 | Cites `classify-changeset.sh:85` for unmatched→CODE; the rule moved. Cite the sentence, not the line (doc r3 W2). |
| C-9 | `g-forge-dev-…-r2.md:151` | Superseded GREEN verdict row for probe C stands above the two HQ addenda that overturn it (gitignored record; informational). |

