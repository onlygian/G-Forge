# M53 — v2.6 Token Diet

**Status:** ✅ Complete (2026-09-02 — v2.6.0 cut)
**Version:** v2.6.0
**Goal:** Same governance, a fraction of the tokens — cut the harness's ~27× token multiplier with zero quality loss: no gate removed, no round capped, no verdict literal changed, no knowledge deleted (ADR-014).
**Process note:** Executed under the ADR-014 §3 process exception — session-model workflow orchestration (inventory → implement → adversarial verify), not the /g-* pipeline, because this milestone rewrites that pipeline's own skills. Commit gate, suites, and the durable record still applied.

## Mechanisms shipped

| # | Mechanism | Where |
|---|---|---|
| 1 | Prose→scripts (KEY:-contract bash, always exit 0) | 20+ scripts under skills/*/scripts/ (doctor checks, review pack, stack detect, sync check, plan/execute/patterns/forecast/init/adr helpers) |
| 2 | Lazy references (essays move, load triggers named) | skills/*/references/, rules/references/, agents/references/ — 113 files, ~42k words preserved |
| 3 | Review pack + delta rounds (one diff derivation, delta re-review with closed-set full-round escape) | skills/g-review/scripts/build-review-pack.sh + g-review/g-doc-review/g-afk + 5 reviewer agents |
| 4 | Model economy (pinned model:/effort:, HES gate, per-lane bumps) | rules/dispatch-matrix.md (canonical), A1 rule, spec-writer HES copy, g-execute/g-refactor tier gates (ADR-016) |
| 5 | Banner-on-delta + rules diet | hooks/workflow-checkpoint.sh (delta-gated stable slice, compressed escalations), rules/g-rules A–J core/reference split |
| 6 | g-specialize diet (kept, not killed — ADR-015) | detect-stack.sh + derive-owns.sh + 48 architect dedups; profiles corpus intact |

## Measured (words, HEAD 788f0f1 → working tree)

- SKILL.md cores (38): 84,205 → 47,925 (−43%; specced heavy skills −60…−84%)
- Per-session rules chain (G-RULES + A–J): 8,884 → 7,088 (−20%; design target ~5.5k — normative cores set the floor)
- agents/ (19): 14,292 → 11,385 (−20%) · architect profiles (49): 41,718 → 34,716 (−17%)
- Banner path: full print on prompt 1 / state change only thereafter; escalations still every prompt, reworded (~40% shorter)
- Suites: 772/0 across 24 (baseline) → 1,244/0 across 37 (13 new script suites + delta-banner cases)

## Done condition

All suites green; adversarial no-knowledge-lost verification pass clean or all findings fixed; structural validation clean (frontmatter/Announce/Steps/Rules, descriptions byte-identical); dispatch-matrix pin test green; CHANGELOG + README + ADRs 014–016 land; version bump 2.6.0, tag, GitHub release.

## Carry / backlog

- Token accounting per dispatch (measure the real multiplier) — backlog (ADR-014 consequence).
- Generate-on-miss architecture card for unsupported stacks (context7-assisted) — backlog (ADR-015).
- Downgrade-evidence replays for doc-reviewer/architecture-enforcer tiers — backlog (ADR-016 §2 protocol).
- Rules-chain deeper cut toward the ~5.5k design target; architect dedup second pass — 2.6.x candidates.
- Session-file reaper: .claude/banner-hash.* joins session-prompt-count.* accumulation debt.
- Unspecced mid-size skills (g-help, g-telemetry, g-roundtable, g-onboard, g-docs, g-audit, g-optimize, …) — 2.6.x diet candidates.
