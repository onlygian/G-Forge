# Retro — 2026-08-22 — M48d AFK run (waves 1-4 + review r1-r2)

## What happened

- **M48d executed end-to-end in AFK mode** off the approved plan (`g-docs/plans/m48d-direct-runner-and-gate-coverage.md`): 4 waves, 6 tasks, all complete. Full suite attested **609/0 across 22 suites** (wave-3 record; HQ re-summed independently, match).
- Wave 1 incidents, all recovered: task-decomposer Write-grant stall (content returned inline, HQ persisted — M51 item 4 evidence again); §12 fixture defect (fixed attempt 2: `GIT_COMMITTER_DATE`, not `--date` — the hook's `--since` filter reads committer date); **the first pre-commit attestation agent (6.8h run) deleted the untracked new suite file via its cleanup trap** — unrecoverable from git, recreated in a single Write+Bash context (author→runner file handoff eliminated), 15/0 green with probes RED-proven.
- Review arc (dual-lane, scoped per M51 governance): r1 code-lead HOLD 0C/4M/4m + doc DOCS HOLD 5B/4W → fix round (F1 prose incl. directive AFK amendment, F2 tests) → r2 HOLD 0C/4M/5m + DOCS HOLD 6B/3W. Converging (most r1 items genuinely closed, numeric sweep clean) but **the fix round re-minted a defect at a site it edited** (CHANGELOG probe census 16-of-15) — the known minting mechanism, again, with the governance instruments applied.
- Session closed at the r2 boundary on the red context gate (M48c-precedent seam). Remaining work is pinpointed in `g-docs/agent-output/review/{code-lead,doc-reviewer}-2026-08-22-r2.md`.

## Decisions inferred

- **Intermediate releases (developer, explicit "a, yes"):** patch/minor cuts at milestone-family closes; v2.4.1 candidate at M48-family close; 2.5 stays the freeze label. Recorded as ADR-012 Amendment — **which r2 found defective** (contradicts ADR `:58` "one release vehicle", two unswept carriers; also says M51 "cuts" where M41 cuts and M51 sets the bar). Amendment rewrite is fix-round-2 work.
- **AFK runs fix rounds autonomously (developer directive):** `skills/g-afk/SKILL.md` Step 4 rewritten — bounded loop, round cap 3, Three-Strikes escalation. r2 found Step 5 doesn't yet render the trail Step 4 promises — fix-round-2 work.
- **M51 item 10 (intake, option a):** adopter-facing review-progress rendering, voice-profile keyed, verdicts byte-exact; premortem row added.

## Patterns

- **Fix rounds re-mint at the site they edit, even with sweep instruments live** — third consecutive milestone. The census-style prose claim (N of M probed) is the worst offender: any enumeration written during a fix round goes stale within the round. Extend the "no new enumerations in fix rounds" rule to *any counted claim*, not just lists.
- **HQ inline writes skip the governance agents are held to.** The ADR-012 amendment was appended by HQ without a repo-wide sweep — exactly what F1/F2 were mandated to do. If HQ writes prose mid-review, HQ sweeps like a fixer.
- **The author→runner seam is the expensive seam:** one Write-grant stall, two mid-run stalls (SendMessage resume worked both times), one file deletion. Single-context author+run (Write+Bash in one agent) closed in one pass what the split lane failed at twice. Feeds M51 items 1/4.
- Forecast outcomes (reconciled, `g-docs/forecasts/m48d-direct-runner-and-gate-coverage.md`): #1 stale-count **happened (git)**; #2 MSYS timing **did not happen** (mitigation-held: no new bounds authored); #3 fixture/live divergence **did not happen** (production-format fixtures held; case-15 probe gap is a marker issue, not divergence); #4 confabulated totals **partial (git)** — attestation sums held everywhere, but the CHANGELOG probe census was confabulated; #5 run-loss **did not happen** (mitigation-held: nohup+disown+no-pipe).

## Cold-start context

**Branch:** main · **Active milestone:** M48 (a✅ b✅ c✅ d — waves done, review mid-arc at r2)
**Tree:** all M48d work UNCOMMITTED in the working tree (commit gate holds until MERGE READY + DOCS READY). All suites green: 609/0/22 attested + fix-round re-runs 83/11/15.
**Next:** fix round 2 (4 pinpointed Majors: CHANGELOG:15 probe census · g-afk Step 5 trail rendering · ADR-012 amendment rewrite + sweep · case-15 probe via runner) + minors per r2 records → r3 (round cap — not converged there means human escalation) → ADR-004 sentinels → commit.
**Key records:** review r1/r2 at `g-docs/agent-output/review/*-2026-08-22-r{1,2}.md` · wave records `g-docs/agent-output/wave-{1..4}/` · plan Progress table current.
