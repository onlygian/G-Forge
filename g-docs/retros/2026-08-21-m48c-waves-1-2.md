# Retro: m48c-waves-1-2 — 2026-08-21

## What was done
- `/g-resume` re-hydration + `/g-update` realign: two installed copies were stale vs canonical (`.claude/hooks/lib/stdin-read.sh` missing the M48b override block, `.claude/rules/g-rules-H-testing.md` missing the falsifiability rule) — both realigned, everything else byte-identical.
- M48c planned: 9 tasks / 5 waves (`g-docs/plans/m48c-code-gate-teeth-and-new-suites.md`), forecast written (`g-docs/forecasts/m48c-code-gate-teeth-and-new-suites.md`, 85% High with hit-rate-0.28 context). Budget gate fired (~43 est vs ~35 remaining); developer approved with a planned seam after Wave 2.
- Wave 1 (complete): fix-closure sweep contract added to `agents/code-lead.md` (:55-66 + Rules cross-ref :112); `GF_STDIN_TIMEOUT_OVERRIDE`/`GF_FAST_STDIN_GUARD_MS` wired into `tests/test-class-split-invariant.sh` six-hook loop (38/0 green, scratch probe RED); new `tests/test-version-agreement.sh` (H4) authored and HQ-attested `Results: 3 passed, 0 failed`.
- Wave 2 (complete): round-ordinal record minting (`code-lead-[date]-r[N].md`) + required sweep-record check + round-3 consolidation note wired into `skills/g-review/SKILL.md` (severity suite 9/9 before and after); override wired into `tests/test-check-commit.sh` cases 23/25 (25/0 green, probe RED on exactly 23/25, case 24 untouched); new `tests/test-router-skill-parity.sh` (H5) green on attempt 3 — HQ-attested `Results: 6 passed, 0 failed`, both derived sets 38/38.
- All work uncommitted by design — commit follows `/g-review` in the resumed session.

## Decisions made
- Seam after Wave 2 accepted at the budget gate (developer choice at plan approval) and taken when the amber gate fired at exactly that boundary — a planned handoff, not a forced one.
- H5 suite attempt 3 escalated to Opus per §A8 after two same-class live-path normalization defects (missing `g-` mapping; unstripped backticks) — `.claude/escalation-log` entry `2026-08-21 m48c-t7-router-parity-suite retry-2`. The working mechanism was canonicalize-both-sets + a shape self-check that fails at extraction.

## Patterns
### Worked well
- HQ-executes-what-test-writer-authors caught both H5 defects immediately — the WRITTEN→run gate turned would-be latent suite bugs into same-turn fix loops (journal: test-writer stops at 07:55/07:58/08:05, each followed by an HQ run).
- Falsifiability probes ran RED in scratch copies on both override wirings before DONE was accepted (T3: 6 failures neutered; T4: exactly cases 23/25 red at a tightened bound) — no green-while-broken shipped.
- One record-write stall (T3, ~1-in-3 class held at 1-of-7) resumed with a single SendMessage — Interrupted ≠ FAILED carve-out worked as documented.
- Wave-close integrity checks clean both waves: tracked delta exactly the scoped files.

### Avoid / do differently
- A derived-set test's live path and synthetic path must share one extraction+canonicalization function from attempt 1 — H5's attempts 1-2 each passed their synthetic cases while the live path broke on a format detail (backticked tokens), because the fixtures didn't exercise the real parser's input format. Write synthetic fixtures in the production format, and add a shape assertion on derived sets so extraction bugs fail loudly at extraction.

## Cold-start context
**Branch:** main
**Active milestone:** M48 — Review-Pipeline Hardening (M48a ✅, M48b ✅, M48c waves 1-2 of 5 done, uncommitted)
**Next up:** Resume M48c at Wave 3 — task 5, revalidate `GF_FAST_STDIN_GUARD_MS` from real override-wired runs (HQ-inline), then Wave 4 (task 8, suite-count baseline + CLAUDE.md re-sum from a detached full run), Wave 5 (task 9, doc closeout), then `/g-review` and the gated commit.
**Key files touched:** code-lead.md, SKILL.md (g-review), test-class-split-invariant.sh, test-check-commit.sh, test-version-agreement.sh, test-router-skill-parity.sh, stdin-read.sh (installed copy), g-rules-H-testing.md (installed copy)
**Carry-over context:** Working tree carries all wave 1-2 edits uncommitted; suite 581/0/19 attestation is INVALID (tree changed — two new suites exist, two fixtures rewired); plan Progress table current through Wave 2; forecast Outcome rows 4-5 filled early (journal evidence), 1-3 reconcile at close.

## Journal basis
111 agent · 2 commit · 1 push · 1 session (2026-08-21; the 2 commits + push are the prior pass's M48b/M51 close, pre-dating this session)
