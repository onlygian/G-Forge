# Retro: v25-minimal-freeze-session-b-waves45 — 2026-08-29

## What was done
- `/g-resume` re-hydrated from `7beeabf` (Freshness `unsynced — no upstream`; branch 2 ahead of `origin/main`, 0 behind). `/g-update` self-host realign: 10 `.claude/rules/` files re-copied (only `A-session` and `I-project-tracking` carried substantive drift, the rest CRLF-only); hooks, libs, pre-commit, settings, architect, implementer all already identical.
- **Wave 4 executed** (`/g-execute 4`, 5 × `claude-plugin-implementer`, all DONE first attempt, no stalls): Task 6 `.github/workflows/tests.yml`; Task 6b `tests/test-readme-counts.sh` + `EXPECTED_SUITE_COUNT` 23→24; Task 7 g-plan wave-planner `output_file` (HQ writes) + bare-`%` sweep + light-tier sentence, wave-planner body coherent; Task 8 g-refactor spec-writer `output_file` (HQ writes) + spec-writer body coherent; Task 9 light-tier sentence in g-help + integration-tiers.
- **Wave 5 executed** (4 implementers; Tasks 10 and 11 hit the 30-turn limit once each, resumed not redeployed, both finished on the resume): Task 10 `.claude/skills/architecture-*/SKILL.md` in g-doctor Check 16 drift set + g-update Step 6c + 4 drift-suite tests; Task 11 agent-lifecycle empty-payload jq guard + 7 tests; Task 12 observe.sh escape-aware sed extraction + 1 test; Task 13 g-skill-validate three-tool-class rewrite. Task 10b by HQ: local architecture skill body regenerated, byte-hash equal to the profile.
- **Tier 1 attested by HQ:** `bash tests/run-all.sh` → 661 passed / 0 failed across 24 suites, hand-summed from the per-suite table = runner total (was 645/23). g-dev fixtures 95/0 (`g-forge-dev`, independently summed). Every new guard test carries a dated `falsifiability:` marker with its RED probe output in the wave record.
- **Gate rounds:** code-lead r1 MERGE READY (9 minor) → r2 HOLD (1 major: architect profile `:16` one-class carrier) → r3 MERGE READY (5 minor). doc-reviewer r1 DOCS HOLD (5B/10W) → r2 HOLD (1B: architect `:16/:37/:71` twins) → r3 HOLD (1B: fix extended report-only to the diagnostic class — a minted fact) → r4 HOLD (2B, both pre-existing: `:37` "proposes" verb falsified by four shipped reviewers; the three-class partition in `architecture.md:6,:19` has no slot for the `Agent(...)` dispatch grant). Three HQ fix rounds applied inline: tests/README 24/661; env-vars `GF_README_PATH` entry + count dropped; architect `:16/:35/:37/:71` three-class rewrite (twice); spec-writer/g-refactor stack-generic cite; todo row 6 + patterns-deferred wave-planner/spec-writer struck. r1 blockers 1, 2, 4, 5 and the r2/r3 blockers are CLOSED with sweep evidence in the r2–r4 records.
- **Nothing committed.** Session stopped at the §A7 gate (exchange 22, amber since 15) with the tree dirty (22 paths) and both sentinels absent.

## Decisions made
- Stopped at r4 rather than open a fifth round: the r4 blockers are changes to shipped authority text (`profiles/claude-plugin/rules/architecture.md:6,:19` and the architect profile's inherited verb), not this wave's edits — evidence: doc r4 record; all four cited reviewer agents' recommendation sections predate this branch.
- Code r3 verdict held for the markdown-only r4 delta without a code r4, on the prior pass's precedent (code r3 / doc r11) — evidence: prior handoff.
- The installed `.claude/agents/claude-plugin-architect.md` was realigned by hand-copy from the profile three times rather than via `/g-update` — evidence: gate records name the copy as byte-identical each round.

## Patterns
### Worked well
- Wave dispatch with exact `file:line` refs and an explicit "read the whole target sentence" clause: 9 of 9 wave tasks DONE on the first attempt, zero FAILED, zero redeploys.
- Interrupted ≠ FAILED honoured: two 30-turn stalls resumed with an output-file-first message; both finished in one resume round-trip.
- HQ attestation caught nothing confabulated this time — every agent-reported count matched HQ's own runs.
- Running the two gates in parallel on the frozen tree saved a round per fix cycle.
### Avoid / do differently
- **A fix that reads only the target bullet mints the next round's blocker.** The `:35` fix left `:16`/`:37`/`:71` (same file, same fact) — r2 HOLD on both gates. Then the `:16`/`:37` rewrite minted "diagnostic = report-only" — r3 HOLD. The prior pass's rule ("read the whole target row/sentence") is too narrow: read the whole *file* for the fact, and derive replacement wording from the authority text, never compose it.
- **Doc-gate rounds widen into shipped-rule defects by round 3–4.** Four rounds moved from this wave's edits to `architecture.md` itself. When a round's blockers are all pre-existing, stop and schedule a rule-text task instead of patching inside the gate.
- **The §A7 amber fired at 15 and the commit boundary was not reached by 22** — same shape as the prior session (closed at 19). A five-task wave + four-task wave + a multi-round gate does not fit one session under the current thresholds; split the gate into its own session or budget one wave per session.
- `rm -rf` on the scratchpad probe dir was denied twice (agent) and once (HQ); the leftover `probe/` directory is harmless. Probe scripts should write under a per-run `mktemp -d` and clean up inside the same process.

## Cold-start context
**Branch:** `chore/v2.5-minimal-freeze` — 2 commits ahead of `origin/main` (`59c7c63`, `7beeabf`), 0 behind, never pushed, no upstream; **tree dirty: 22 paths uncommitted** (Waves 4–5 + three fix rounds), both sentinels absent, `.claude/review-holds` 1.
**Active milestone:** M52 — v2.5 Minimal Freeze 🔄 In progress (Session B waves executed and Tier-1 green; Session B gate open at doc r4 HOLD / code r3 MERGE READY; Session C and D still owed; release target Sunday 2026-08-30)
**Next up:** · **Doc r5 fix round, then commit** — close the two r4 blockers: (a) `profiles/claude-plugin/agents/claude-plugin-architect.md:37` drop "proposes" so the reviewer-class trigger is *executes* fixes (and re-check `g-skill-validate:71` says the same); (b) the `Agent(...)` dispatch grant — decide whether `profiles/claude-plugin/rules/architecture.md:6,:19` gains an "orthogonal dispatch grant" clause or whether architect `:16/:35` + `g-skill-validate:81` scope their partition to the three tool grants only; this edits authority text, so read `architecture.md` whole and the four grant holders (`review-orchestrator`, `project-manager`, `feature-implementer`, `templates/stack-implementer.md:31`) first. Then re-copy the installed architect, doc-reviewer r5 with closure sweep, code-lead r4 if the rules file changed, `git add -u` + `git add .github tests/test-readme-counts.sh`, both sentinels off one `write-tree`, commit. · `/g-update` after the commit (hooks `agent-lifecycle.sh`, `observe.sh` drift; installed rules). · Carry the nine code r1 minors + code r3's 5 into `g-docs/todo.md` (F6 CI `permissions`/`concurrency` before first push). · Session C: Wave 6 (Task 14, forecast relabel Task 16 + its four co-sites, M40 REFERENCE classifier Task 17), Wave 7, Wave 8 CHANGELOG `[2.5.0]` covering Tasks 1–23. · Session D 2026-08-30: read `communication-plan-2.5.md`; bump, §D step 5 sweep, tag, release, freeze.
**Key files touched:** tests.yml, test-readme-counts.sh, test-run-all.sh, SKILL.md (g-plan, g-refactor, g-help, g-doctor, g-update, g-skill-validate), wave-planner.md, spec-writer.md, integration-tiers.md, agent-lifecycle.sh, observe.sh, test-agent-lifecycle.sh, test-observe.sh, test-g-doctor-drift.sh, README.md (tests/), env-vars.md, claude-plugin-architect.md, patterns-deferred.md, todo.md, v25-minimal-freeze.md (plan, gitignored), CLAUDE.md (gitignored)
**Carry-over context:** Gate records this session (gitignored): `agent-output/review/code-lead-2026-08-29-v25-session-b-waves45-r1..r3.md`, `doc-reviewer-2026-08-29-v25-session-b-waves45-r1..r4.md`, `g-forge-dev-2026-08-29-v25-session-b-waves45-r1.md`, diff patches `diff-2026-08-29-session-b-waves45{,-fixround,-fixround2,-fixround3}.patch`; wave records `agent-output/wave-4/task-*.md`, `wave-5/task-*.md` (probe outputs live there). Carried doc WARNINGs (advisory, 11): g-forecast relabel co-sites (`g-forecast:3`, `README:369`, `voice-profiles:28/:56`) · CHANGELOG `[Unreleased]` has no Tasks 6–13 lines (Task 19 folds them) · `tests/README.md:1` "Hook tests" · CI workflow undocumented in README · `tests.yml:14` cites the gitignored `.claude/rules/` path · `tests.yml:24` cites a gitignored task record · `g-refactor:166` "spec-writer writes specs" · `g-help:19` `/g-tier light` vs `:20` retired bare form · no ADR for first CI · `I-project-tracking.md:38` agent-output "Written by" column. Ship-as-is set (developer, 2026-08-29) unchanged. Telemetry cautious · voice gian · last-trim 2026-08-18. Forecast `v25-minimal-freeze.md` Outcome table: scenario 4 still `unverified` (Session C). Observer gap unchanged: HQ suite runs not journaled.

## Journal basis
161 agent · 1 session · 0 commit · 0 test (since session open 19:03Z; HQ `run-all.sh` runs are not journaled)
