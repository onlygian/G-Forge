# Retro: v25-minimal-freeze-session-f2r — 2026-08-30

## What was done

- **F2 redo audit on the session model (developer directive):** the 12-file F2 slice re-read whole from the dirty tree, verdicts per F2 finding — **7 KEEP, 0 revert, 4 extended** (F2-1/2/4/6) — plus six new Minors (R-1…R-6), recorded in `g-docs/audits/2026-08-30-fable-f2-redo-survives-agents-record-seam.md`. Headline new finding: `/g-resume` shipped three drifted hook/skill cross-refs, none test-pinned (R-1).
- **Wave F2-R (Tasks 35–37, three implementer dispatches, files disjoint, all DONE first attempt):** PM dispatched-mode residue closed (ask-and-wait splits, no-skill-tool preamble, grant trimmed to `Agent(code-lead, pr-writer)`, `BLOCKED` trigger); `/g-resume` refs corrected + same-day compact-state supersession rule; `/g-retro` Next-up derivation re-ordered (directive → plan wave → milestone task → todo row → carried) with its read-list sources added and the phantom `note` kind dropped; `/g-doc-review` mainline `<remote>` bound; doc-writer `BLOCKED` + `README GAP:`; repo-only pointers dropped; `tests/test-review-severity.sh` 21→22 assertions in the wave, 23 after the fix round's positive pin, with §H markers.
- **Gates closed:** attestation 733/0 then post-fix 734/0 across 24 suites (both HQ-summed against their tables); new-guard neuter probe GREEN 22/0 → RED 21/1. Code gate r1 HOLD (1 Major · 6 Minor) → r2 MERGE READY (0/0/2, both closed in the same round-trip); doc gate r1 HOLD 4B/6W → r2 HOLD 1B/2W → r3 DOCS READY 0B/2W advisory. Every closure carries recorded sweep evidence.
- **Committed:** `45641b7` (Wave F2 + F2-R + fix rounds, both sentinels, gate consumed them). One HQ revert inside the wave: an implementer's disclosed extra hunk modernizing a released `[2.4.1]` CHANGELOG citation — released sections are append-only history.

## Decisions made

- **F2-4 derivation order amended per the F2 retro's own evidence** — the developer-directive-first chain replaced the todo-lead-row-first chain the F2 wave shipped (commit `45641b7`, `skills/g-retro/SKILL.md:47`).
- **PM grant trimmed rather than documented** — `task-decomposer`/`wave-planner`/`spec-writer` grants had zero body uses after F2-2's routing fix and invited the removed inline pipeline back; carrier sweep showed no dependent surface (commit `45641b7`).
- **Minor 6 closed by a positive pin, not a case-insensitive grep** — a `grep -qi` absence guard would false-RED on the legitimate lowercase qualified line; the qualifier's presence is pinned directly instead (`tests/test-review-severity.sh`).
- **Released CHANGELOG sections held append-only** — the `[2.4.1]` `g-plan:105` citation restored after a fix round modernized it (the file's own recorded convention).

## Patterns

### Worked well
- Attestation dispatched the moment the test file existed (F2 retro's rule) — it returned before the gates needed it.
- 9/9 dispatches wrote their `output_file` first attempt; HQ verified each path on disk before accepting (forecast scenario 2, mitigation held again).
- Scoped gates: code r1 and r2 each returned in one read of a frozen patch + named files; no unscoped repo re-derivation (todo 11's riders applied).

### Avoid / do differently
- **A fix round minted the next round's defect, twice, in HQ's own records** — the r1 code Major was the audit record's "the rest … were verified accurate" completeness sentence (§B hard-stop class, false on disk: the enumerated set had eight members, the list seven); the r2 doc blocker/warnings were the fix round's own additions (numbering key, attestation figures overtaken by the round's added assertion). Per-ref statements and committing the fresh runner line are what broke the chain (forecast scenario 1, third consecutive session).
- **Dispatch prompts must separate REPLACEMENT TEXT from RATIONALE** — Task 36's implementer pasted the prompt's "WHY this order" paragraph verbatim into shipped skill text, and the prompt's own `— :` typo shipped too; both HQ's bugs, both fixed inline.
- **The observer's test patterns miss `bash tests/*.sh`** — today's journal holds 0 `test` events against 4 real full-suite runs (known gap, carried).

## Cold-start context
**Branch:** `chore/v2.5-minimal-freeze` — ahead of `origin/main` by the ten prior commits + `45641b7`, plus this pass-close commit when it lands; never pushed, no upstream. Tree clean after `45641b7` except this retro, the handoff rewrite, and the forecast reconciliation (the pass-close set).
**Active milestone:** M52 — v2.5 Minimal Freeze 🔄 In progress (A, B, C, F1, F2+F2-R closed; F3 then Session D owed).
**Next up:** **F3 audit cycle** — derived from the active plan/milestone (no explicit next-step directive this session): `/g-resume`, then the remaining SURVIVES skills slice (`g-roadmap`, `g-intake`, `g-align`, `g-brief`, `g-kickoff`, `g-onboard`, `g-forecast`, `g-blast-radius`, `g-patterns`, `g-identity`, `g-wiki`, `g-tier`, `g-train`, `g-skill-design`, `g-skill-validate`, `g-roundtable`) + the open ledger (todo 5, 6, 9, 14, 15, 16, 18 — row 11 deliberately outside: M51/G-Proof design input, per the M52 entry). Same cycle shape: audit record → filter → one wave → attestation early → gates → commit → retro. **Pending developer decision (asked mid-session, unanswered):** two proposed Session D adds — point F3's doc gate at README explicitly, and a `/g-doctor` release-hygiene precondition (gitignore/strays/drift) before the bump. *(Decided 2026-08-30, after this retro was written: both adds approved, the README re-check broadened to README + `g-docs/agents.md` — see the M52 "two adds approved, and F3's bar stated" amendment at F2-R close.)*
**Handoff at retro:** · **Redo the F2 audit on Fable 5 Extra, fresh session, tree as is** — `/g-resume`, read the same 12-file slice (the F2 fixes are on disk uncommitted: `git diff` shows them, `git show HEAD:<path>` the pre-wave text), write `g-docs/audits/2026-08-30-fable-f2-redo-….md`, diff against the F2 record, decide per finding: keep / extend / revert the landed fix. · Then: falsifiability markers on the three new guards (probes already RED — `g-forge-dev-2026-08-30-f2-r1.md`) · `/g-review` + `/g-doc-review` on the whole wave · commit · `/g-retro` · handoff. · Then F3 (remaining SURVIVES skills + todo 5, 6, 9, 14, 15, 16, 18), then Session D (read `communication-plan-2.5.md` · Task 20 bump · Task 21 sweep · merge `main` `--no-ff` + push · Task 22 tag + release · freeze). Nudges due: `/g-trim` (last 2026-08-18), `/g-align`, M52-close `/g-wiki`.
**Key files touched:** project-manager.md, test-writer.md, doc-writer.md, error-detective.md, architecture-enforcer.md, code-reviewer.md, dependency-auditor.md, doc-reviewer.md, security-auditor.md, SKILL.md (g-adr, g-doc-review, g-forecast, g-resume, g-retro — all in `45641b7`), test-review-severity.sh, CHANGELOG.md, agents.md, todo.md, 2026-08-30-fable-f2-redo-survives-agents-record-seam.md, ROADMAP.md, v25-minimal-freeze.md (the forecast record — this pass-close set)
**Carry-over context:** Records (gitignored): wave `agent-output/wave-f2r/task-{35,36,37}*.md`; attestation `agent-output/review/g-forge-dev-2026-08-30-f2r-r1.md`; gates `code-lead-2026-08-30-f2-redo-wave-r{1,2}.md`, `doc-reviewer-2026-08-30-f2-redo-wave-r{1,2,3}.md`; frozen diffs `diff-2026-08-30-f2-redo-wave{,-fixround}.patch`. Doc r3's two advisory warnings carried: the Gate-rounds key infers "Minor 2" positionally, and the named fixround log lives in the session scratchpad (runner line committed verbatim beside it). `review-holds` 0 (1 up, 1 down, both evidenced). `.claude/hooks/pre-commit` realigned from source mid-review (comment-only drift). Nudges due: `/g-trim`, `/g-align`, M52-close `/g-wiki`.

## Journal basis
Today's journal (spans sessions F2 and F2-R): 6 session · 674 agent · 9 commit · 1 destructive · 0 test (the observer's pattern set misses this repo's `bash tests/*.sh` runs — 4 real suite runs today are unjournaled, known gap) — 690 events read.
