# Retro: v25-minimal-freeze-gates-pass1 — 2026-08-29

## What was done
- Re-entered via `/g-resume` (no upstream on the branch; record axis not behind `origin/main`), then ran the two merge gates over Session A/B's uncommitted changeset until both cleared on one frozen tree, and committed it: `59c7c63`, 35 files (+894/−246), the first commit on `chore/v2.5-minimal-freeze`.
- Doc gate rounds 4–11 (records `doc-reviewer-2026-08-29-v25-minimal-freeze-r3.md` … `-r10.md`): r4 2B/14W · r5 1B/11W · r6 1B/10W · r7 1B/10W · r8 1B/9W · r9 1B/9W · r10 1B/9W · **r11 DOCS READY 0B/10W**. Every blocker fixed HQ inline; every closure re-swept by the next round with grep evidence in its record.
- Code gate rounds 1–3 (`code-lead-2026-08-29-v25-minimal-freeze-r1..r3.md`): r1 HOLD 0C/3M/3m (review-panel wiring claimed live at `skills/g-review/SKILL.md:24`, `g-docs/telemetry-metrics.md:100` + profile table, `tests/test-review-severity.sh` header) · r2 HOLD 0C/2M/3m (the same claim re-minted at `g-review:135`; `skills/g-telemetry/SKILL.md:129` worked example) · **r3 MERGE READY, 8/8 done conditions**. `.claude/review-holds` 0→1→2→0 under the new single-owner contract; the decrement was gated on Step 4b evidence in the r3 record.
- Tier 1 attested three times (before code r1, after r1 fixes, after r2 fixes): 645/645 across 23 suites, the per-suite table hand-summed each run and matching the grand total; `g-dev/fixtures/` 95/0 via `g-forge-dev`. Installed-copy drift (hooks + libs): clean.
- Record fixes landed on the way: the retired "`g-proof-roadmap.md` is gitignored / hand-carried" fact swept at ten sites (`ROADMAP.md:625`, `project_brief.md:40,:67`, `ADR-012:125,:136`, five `milestones/M29/M33/M34/M35/M44:3` stamps); the deferred reviewer-scoped-Write pattern retargeted along its whole chain (`CHANGELOG.md:11` → `patterns-deferred.md:4` → `todo.md` row 6, the row's own drifted counts omitted per ADR-013); handoff dirty-tree count omitted; `[Unreleased]` entries added for Task 23, Task 15's §A7 rule text, the G-RULES §I `field-reports/` row, and the solo-panel disclosure; `rules/g-rules/I-project-tracking.md` gained the `field-reports/` row.
- Developer decision at r4: round-5 scope = blockers + the five `FORK-BOUND` milestone stamps + W-6/W-9, with r4's W-10..W-14 recorded ship-as-is in the handoff by `file:line`.

## Decisions made
- **Fix rounds wait for every in-flight gate.** Code r2 found doc r6's fixes landing under it (tree mutated mid-review); from then on fixes were held until all dispatched gates returned, and code r3 + doc r8 ran in parallel on a frozen tree (code r3 recorded the newest mtime unchanged across every read).
- **The Next-up doc-review item no longer names a round ordinal** (`ROADMAP.md:8`). The ordinal was found stale at rounds 5, 7, 8, 9 and the r10 fix minted a count inside the sentence removing it; the class was closed by deleting the number, not advancing it.
- **Tier 3 smoke test skipped** on the MERGE READY path per the project's recorded testing model (release-and-dogfood; sandbox-proven + field reports), stated at the gate rather than silently.
- **`/plugins`, not `/g-update`, is the consumer vehicle for shipped agents** — `skills/g-update/SKILL.md` Step 2 handles only `*-architect`/`*-implementer`; the CHANGELOG entry that said otherwise was corrected at doc r8.

## Patterns
### Worked well
- **Twelve dispatches, twelve records written first attempt and read back at the dispatched path** (8 doc-reviewer, 3 code-lead, 1 g-forge-dev — journal start/stop pairs match). The "write your stub first" instruction held every time.
- **Every attested total was hand-summed from the runner's 23-row table before acceptance**, three runs; all matched.
- **HQ spot-checked both r4 blockers independently before fixing** (`ROADMAP.md:625` vs `:617`; `git status` 17 M + 1 RM + 6 untracked = 24 vs the handoff's 23) — no fix was applied on a reviewer's word alone.
- **Frozen-tree parallel gating** after the r2 process finding: code r3 came back MERGE READY with zero mid-review mutation, and doc r8 ran alongside it.
- **A false fact caught by HQ's own grep before dispatch** — the first CHANGELOG draft credited `/g-retro` with the heading-level fix; `skills/g-retro/SKILL.md` was not in the changeset, the sentence was corrected before r5 saw it.

### Avoid / do differently
- **Eight doc rounds, and every blocker from r5 on was minted by the previous round's fix.** r5: `CHANGELOG:11` retargeted to M52 without reading M52's `Absorbs:` line. r6: retargeted to `todo.md` row 6 without reading row 6 to its end ("lands as M51 item 4"). r7: row 6's own "12 agents / 11 remain" counts. r8: a `/g-update`-resyncs-agents claim never checked against `skills/g-update`. r9: an ordinal bumped instead of removed. r10: "four consecutive rounds" inside the sentence written to remove a count. One rule covers all six: **read the whole target you point at, and write zero new facts inside a fix** — a fix that only deletes cannot mint.
- **A fix round landed under an in-flight gate** (doc r6 during code r2). Code-lead re-read every load-bearing site in one snapshot and the verdict survived, but it was luck. Freeze the tree until every dispatched gate has returned.
- **`perl -i` with `$` anchors silently no-ops on CRLF files** — two CHANGELOG substitutions matched nothing and only the verify-grep exposed it; the Edit tool was used instead. Verify every substitution with a count, never assume.
- **HQ test runs are invisible to the observer** — three `bash tests/run-all.sh` runs redirected to a scratch log produced zero `test` journal events (journal: 1 commit, 0 test). The retro's "tests before commits" signal is blind to the way this repo actually runs its suite.
- **Context gate: the amber banner fired at exchange 15 and the session ran to ~19 to reach the commit boundary.** The model cannot read `/context`; on Remote Control the developer pastes it. The banner text itself still says "remaining capacity drops below 25%" — Task 14's inversion, contradicting the rule it enforces.
- **Round numbering resets at midnight** — this target's series is `2026-08-28-r1` then `2026-08-29-r1..r10`; the eleven rounds read as "r10" on disk. Unchanged from Session B's note.

## Cold-start context
**Branch:** `chore/v2.5-minimal-freeze` — one commit ahead of `main` (`59c7c63`), never pushed, no upstream configured
**Active milestone:** M52 — v2.5 Minimal Freeze 🔄 (Session B's gate pass closed; Session B Waves 4–5 still to run; Session C Saturday PM; Session D Sunday 2026-08-30 release)
**Next up:** · **Commit this retro + handoff** (doc-only; needs `/g-doc-review` → `.claude/g-forge-docs-approved`) — or fold them into the next gated commit. · **`/g-update`** — the installed `.claude/rules/g-rules-A-session.md` and `g-rules-I-project-tracking.md` copies lag the corrected source (§D step 9 precondition for the tag; not a merge blocker). · **Session B Waves 4–5** — `/g-execute 4`: Tasks 6, 6b, 7, 8, 9, 10, 11, 12, 13 (`g-docs/plans/v25-minimal-freeze.md`); then `/g-review` + `/g-doc-review`, commit. · **Session C** — Wave 6: Task 14 (`hooks/workflow-checkpoint.sh:271,:279` + `tests/test-workflow-checkpoint.sh:462` pinned as a *direction* assertion, not digits), forecast relabel, M40 REFERENCE classifier + tests + falsifiability probe; Wave 7: local architecture-skill copy; Wave 8: CHANGELOG `[2.5.0]` (Task 19 done condition now "Tasks 1–23"; fold the `[Unreleased]` entries). · **Session D** — read `g-docs/communication-plan-2.5.md` first; bump, §D step 5 grep sweep, tag `v2.5.0`, release, freeze. Re-block conditions recorded in the doc-gate records for Session D: comms/README present tense for Tasks 16/17 (`comms:68,:70`, `README:44,:46`); `CHANGELOG:28` distributive attribution.
**Key files touched:** ROADMAP.md, project_brief.md, CHANGELOG.md, todo.md, patterns-deferred.md, telemetry-metrics.md, 012-g-forge-2.5-final-release-scope.md, M29/M33/M34/M35/M44 milestone stamps, I-project-tracking.md, A-session.md, g-review/SKILL.md, g-telemetry/SKILL.md, g-execute/SKILL.md, code-lead.md, review-orchestrator.md, test-review-severity.sh, test-telemetry-contract.sh, test-run-all.sh (plus the Session A/B set carried into the same commit)
**Carry-over context:** Tree clean after `59c7c63` except this retro and the rewritten handoff (uncommitted). Both sentinels cleared by the post-commit hook. `.claude/review-holds` 0. Telemetry profile cautious. Voice gian. last-trim 2026-08-18 (weekly `/g-trim` nudge active; `/g-align` nudge active). Doc-gate ship-as-is set (developer, 2026-08-29): comms §7 `:130,:147,:148` · comms/README present tense for Tasks 16/17 · retro-A frozen figures · forecast `:4,:21` · `telemetry-metrics.md` no ship vehicle. Carried doc WARNINGs at r11 (all advisory; W-10 closed by the handoff rewrite): `CHANGELOG:28` distributive attribution · handoff append trail (closed by this rewrite) · forecast `:20` stale deferral target · round-ordinal ID drift in two frozen records · retro-B "all five" · retro-B dirty-tree copy · `ADR-012:36` cite · `tests/README.md:1` "Hook tests" · gitignored plan Wave Schedule.

## Journal basis
Since session open 16:20Z: 144 agent · 1 commit · 1 session (0 test — HQ suite runs are not journaled). Whole day: 186 agent · 1 commit · 3 session.
