# Retro: v25-minimal-freeze-session-b-gate-close — 2026-08-30

## What was done
- Re-hydrated via `/g-resume` (Freshness `unsynced — no upstream`; Record axis not behind `origin/main`); the checkpoint's "handed-off ADR needs verifying" nudge was a false positive — the handoff named no `verify ADR-NNN` task.
- Doc fix round 4 closed both doc r4 blockers at the authority and its two enforcement surfaces: `profiles/claude-plugin/rules/architecture.md:6,:19` (+ `Agent(...)` dispatch grant declared orthogonal to the three tool classes), `profiles/claude-plugin/agents/claude-plugin-architect.md:16,:35,:37` (reviewer-class prohibition reworded from "proposes or executes fixes" to "executes fixes or emits implementation content; a prose suggestion is a finding"), `skills/g-skill-validate/SKILL.md:71,:81,:83`. Retired-wording sweep: 0 hits repo-wide. Diff frozen to `agent-output/review/diff-2026-08-29-session-b-waves45-fixround4.patch`.
- Installed copies realigned by hand and verified byte-identical: `.claude/rules/architecture-claude-plugin.md`, `.claude/agents/claude-plugin-architect.md`, `.claude/skills/architecture-claude-plugin/SKILL.md` (frontmatter + rules body — the Wave 7 residual item, done here), and post-commit `.claude/hooks/agent-lifecycle.sh` + `observe.sh`. `/g-update` was not run; §D step 9's hand-sync branch was used.
- Tier 1: full suite green — 24-row runner table hand-summed to 661 passed / 0 failed, matching the Grand total (two stray suite-internal `Results:` lines excluded), 287s.
- Gates run in parallel on the frozen tree: code-lead r4 (rules-file re-open) MERGE READY 0C/0M/8m (4 new, 4 carried, R3-1 closed with grep evidence; solo review disclosed); doc-reviewer r5 DOCS READY 0B/4W, both r4 blockers CLOSED per line incl. twins, `Agent\(` boundary re-run with zero false failures. Records: `agent-output/review/code-lead-2026-08-29-v25-session-b-waves45-r4.md`, `doc-reviewer-…-r5.md`.
- `g-docs/todo.md` task 15 written: code-gate Minors carried (r1 F1–F9, r3 R3-2/R2-2/R2-3/R2-4, r4 R4-1/R4-2/R4-4), F6 flagged pre-push; R2-2/R2-4 rows extended with the carrier sites fix round 4 added; hand-typed counts dropped (ADR-013).
- `.claude/review-holds` 1 → 0 (the r2 HOLD it counted was closed at r3 with sweep evidence and re-confirmed at r4).
- Committed `9dd49c4` — Waves 4–5 + four fix rounds + the waves retro, 25 paths, both sentinels off one `write-tree`, consumed post-commit, commit journaled.

## Decisions made
- `Agent(...)` dispatch grant is orthogonal to the reviewer/diagnostic/writer classes — a wording clause at the authority, not a fourth class and not an ADR (fix-round-4 patch; doc r5 and code r4 both accepted it, code r4 noting `review-orchestrator` is still unlabelled — todo 15 R4-1).
- Reviewer-class prohibition derived verbatim from the authority (`:6` "findings only", `:19` "never fixes") rather than composed — the retro-B rule applied; closed in one round after four that did not.
- Handoff-only pass-close commit goes through the doc gate like any DOC-bucket change (classifier `hooks/lib/classify-changeset.sh:69`, no record exemption) — same as 7beeabf.

## Patterns
### Worked well
- One fix round closed a four-round thread: read the authority file whole, edit the authority first, derive enforcement wording from it, sweep retired strings to zero before dispatching — both gates closed on the first re-review (journal: doc-reviewer + code-lead start 05:10, stop 05:20/05:21, commit 05:22).
- Gates + full suite run concurrently on the frozen tree; both reviewer dispatches wrote their record on the first attempt, no resume needed.
- Handoff scope for a gate-close session was right-sized: gate + commit only, closed at low context.
### Avoid / do differently
- `.claude/review-holds` was left at 1 across a MERGE READY that closed the counted HOLD (r3, prior session) — the decrement is part of the MERGE READY path, not a later cleanup.
- Observer gap persists: 0 `test` journal entries today although the full suite ran (background HQ Bash is not journaled) — a retro reading the journal alone would conclude no tests ran before the commit.
- The pass-close records typed a count copied from the prior handoff ("11" carried WARNINGs) over an enumeration of 10, and the missing item was the one warning armed to fire at this very commit (pass-close doc gate r1 BLOCKING 1) — counts are re-derived from the list at write time or omitted, never carried forward.
- Handoff line refs go stale on insertion (`g-skill-validate:81` → `:83` after a two-line insert; code r4 R4-3) — a handoff cites the sentence, not only the line, when the site is about to be edited.

## Cold-start context
**Branch:** `chore/v2.5-minimal-freeze` — 3 commits ahead of `origin/main` (`59c7c63`, `7beeabf`, `9dd49c4`), 0 behind, never pushed, no upstream; tree clean after `9dd49c4` except this retro + the handoff rewrite; both sentinels consumed; `.claude/review-holds` 0.
**Active milestone:** M52 — v2.5 Minimal Freeze 🔄 In progress (Session B closed and committed; Session C — Waves 6, 7-residual done, 8 — and Session D release still owed; release target Sunday 2026-08-30, today)
**Next up:** · **Session C — Wave 6** via `/g-execute`: Task 14 (§A7 wording pinned pair, hook + test — reset at ~25% of the window *used*), Task 16 (forecast relabel + four co-sites `g-forecast:3`, `README:369`, `voice-profiles:28/:56`), Task 17 (M40 REFERENCE classifier arm + tests + falsifiability probe). Wave 7's residual (local architecture-skill copy) was regenerated 2026-08-30 and verified byte-identical — confirm at the Session C gate, then mark it in the M52 entry. Wave 8: CHANGELOG `[2.5.0]` covering Tasks 1–23 (folds the carried `[Unreleased]` Tasks 6–13 WARNING). Gate: `/g-review` + `/g-doc-review`. · Before first push: todo 15 F6 (`tests.yml` `permissions: contents: read` + `concurrency:`). · Session D: read `communication-plan-2.5.md` first; version bump both manifests; §D step 5 literal-fact grep sweep; merge to `main` `--no-ff`, push; tag `v2.5.0` + GitHub release with the CHANGELOG section; freeze. · Nudges due: `/g-trim` (last 2026-08-18), `/g-align`.
**Key files touched:** architecture.md, claude-plugin-architect.md, SKILL.md (g-skill-validate), todo.md, review-holds, reentry.md, architecture-claude-plugin.md (installed rules + skill copies), agent-lifecycle.sh + observe.sh (installed hooks), diff-2026-08-29-session-b-waves45-fixround4.patch, code-lead-…-r4.md, doc-reviewer-…-r5.md, ROADMAP.md, 2026-08-30-v25-minimal-freeze-session-b-gate-close.md
**Carry-over context:** Gate records (gitignored): `agent-output/review/code-lead-2026-08-29-v25-session-b-waves45-r1..r4.md`, `doc-reviewer-…-r1..r5.md`, `g-forge-dev-…-r1.md`, diff patches `diff-2026-08-29-session-b-waves45{,-fixround,-fixround2,-fixround3,-fixround4}.patch`; wave records `wave-4/`, `wave-5/`. Carried doc WARNINGs (advisory): r1–r2's set less one — r1's `ROADMAP.md:8` `Next up:` warning (armed by r5 to fire at the handoff-rewrite commit) is CLOSED by this pass's handoff rewrite; the rest unchanged (g-forecast co-sites → Task 16 · CHANGELOG `[Unreleased]` Tasks 6–13 → Wave 8 · `tests/README.md:1` · CI undocumented in README · `tests.yml:14` gitignored-path cite · `tests.yml:24` record cite · `g-refactor:166` · `g-help:19` vs `:20` · no CI ADR · `I-project-tracking.md:38`) + r5's 4 (F2 duplicate and the R2-2/R2-4 new carriers → folded into todo 15; `review-orchestrator` class label unassigned → todo 15 R4-1; `g-wiki/architecture.md:13` false lead clause, outside the changed set → M52-close `/g-wiki` refresh). Code-gate Minors → todo 15. Ship-as-is set (developer, 2026-08-29) unchanged. Forecast `v25-minimal-freeze.md` Outcome: scenario 4 still `unverified` (Task 14, Session C). Telemetry cautious · voice gian · last-trim 2026-08-18. Installed copies: hooks ×2, rules, architect, architecture-skill all realigned by hand 2026-08-30 — `/g-update` not run. Process rules carried: a fix reads the whole file and derives wording from the authority; blockers all pre-existing → schedule a rule-text task, don't patch inside the gate; one wave (+ its gate) per session under current §A7 thresholds. Observer gap: HQ suite runs unjournaled.

## Journal basis
26 agent · 1 commit · 1 session as of the `9dd49c4` commit at 05:22:45Z — the pass-close doc-gate dispatch after it adds its own agent lines (2026-08-30; the 2026-08-29 file was read for the Session B waves context, already retro'd in `2026-08-29-v25-minimal-freeze-session-b-waves45.md`) — 0 test entries although the full suite ran (observer gap).
