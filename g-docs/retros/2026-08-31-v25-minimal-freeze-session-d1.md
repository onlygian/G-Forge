# Retro: v25-minimal-freeze-session-d1 — 2026-08-31

## What was done
- **Session D preconditions closed:** `g-docs/communication-plan-2.5.md` read whole (publish-time re-verify items and the open announcement block-order decision carried forward); `/g-doctor` run — 16/16 required green, zero drift on every surface, versions aligned at 2.4.1 — with its two advisories (Check 16 template-instantiated class; Check 24 bare-prose regions in this repo's gitignored CLAUDE.md) explicitly accepted in the committed record (developer, via the approved recommendation).
- **Todo row 19 fixed and shipped (`e75fac0`):** the commit-gate boundary pad in `hooks/lib/commit-detect.sh` is quote-aware — quoted pathspecs are no longer mutated inside their quotes. Final form: a single-pass `LC_ALL=C` awk scanner (an interim bash-loop version was correct but quadratic and was replaced at the code gate), NEWLINE-FREE contract with its load-bearing caller-ordering precondition stated, row-16 sentinel comment de-falsified with current behavior pinned, PS-QUOTED-PAD fidelity pins for every boundary character, a hand-maintained boundary loop, and a `GF_PAD_TIMING_MS` timing guard (falsifiability probe RED 7325ms vs the 6000ms bound). RED-then-green throughout.
- **Gate arc:** code r1 HOLD (0C/2M/2m) → r2 HOLD (0C/2M/4m) → r3 MERGE READY (0C/0M/3m advisory); doc r1 HOLD (1B/4W) → r2 HOLD (1B/1W) → r3 HOLD (1B/1W) → r4 DOCS READY (0B/2W advisory). Attestations: `g-forge-dev-2026-08-31-d-r2.md` (all 24 suites green, HQ-summed 770/0) and `g-forge-dev-2026-08-31-d-fixtures-r2.md` (five g-dev fixtures green, re-run after the awk rewrite).
- **Ledger:** row 19 closed to `g-docs/todo-done.md` with post-rewrite attestation cites; row 20 opened (ANSI-C-quote chained-commit known limit, surfaced by code gate r1 out-of-scope, rides to G-Proof R0 under the amendment-4 filter).
- **Weekly `/g-trim` run** — CLAUDE.md issues: the suite-count refresh (executed this session), a drifted `project_brief.md` line-cite in transitional-rules, a sparse-section merge candidate; doc-reviewer memory curation flagged, advisory.
- Installed `.claude/hooks/lib/commit-detect.sh` realigned from source and hash-verified; clean at `e75fac0` (installed-copy check in `doc-reviewer-2026-08-31-d-task50-r2.md`, which postdates the last lib edit). CLAUDE.md per-suite line re-derived to 119.

## Decisions made
- Row 19 gates the release rather than riding to G-Proof R0 (developer "Go" on the recommendation; the fix + gates are this session's work).
- The two `/g-doctor` advisories are accepted for v2.5.0, recorded in the todo-done Session D section (developer, same approval).
- The pad's newline divergence is closed by contract truth (precondition stated) rather than code change — the divergence is unreachable via the sole caller (code gate r2/r3 records).
- Claim-repair strategy switched from rewording to deletion at doc r3 — the deletion-form fixes are what closed the arc (gate records).

## Patterns
### Worked well
- RED-then-green held for every new pin (task-50 record, fix-r1 record: probe outputs verbatim).
- HQ-sums-the-table caught nothing false this session because every delegate returned tables that summed clean — and the one stale-attestation cite that did slip into the ledger was caught by the code gate (r2 Major 2).
- Budget-one-resume: the first implementer finished after one resume (corroborated at `wave-d/task-50-quote-aware-pad.md`); the second took one explicit resume plus its own background-notification re-wakes (journal: 5 starts / 3 stops for that agent); the platform-killed code-lead resumed once to a full round with no lost work (journal: second start 15:01:58Z, no intervening stop).
- Scoped gates (named files + declared baseline + frozen diff per round) kept seven review rounds affordable on a five-file diff.
### Avoid / do differently
- **Five false/unpinned claims across the gate arc, two distinct mechanisms** (verbatim in the gate records): four minted in a round's own fix prose (code r1M2, r2M2; doc r1B, doc r2B), and one the inverse — a pre-existing contract sentence falsified by the round's code change beneath it (code r2M1, the awk contract, "unchanged by this round" per its record). Two remedies, both needed: write the minimal true statement or none, and re-read the prose above any code you rewrite. Deletion-form fixes closed the arc; rewording never did.
- An HQ direct-execution attempt (suite runs + busy-loop measurement) was permission-denied and correctly abandoned at first block — the running delegate owned those steps anyway; parallel HQ measurement would have distorted its loaded-timing evidence.
- The fixture attestation went stale silently when the implementation was rewritten mid-arc — re-run the fixture leg after any change inside the path it exercises (caught by HQ this time, but only on review of what the r2 dispatch was about to cite).

## Cold-start context
**Branch:** `chore/v2.5-minimal-freeze` — tree clean at `e75fac0`; ahead of `origin/main` by the full unpushed branch history; never pushed, no upstream.
**Active milestone:** M52 — v2.5 Minimal Freeze 🔄 (A, B, C, F1, F2+F2-R, F3, **D1 closed**; the release cut remains).
**Next up:** **The v2.5.0 cut, in a fresh session** (§A7 amber reset taken at this commit boundary): Task 20 version bump — `plugin.json:4` + `marketplace.json:12` to 2.5.0 same commit, `README.md:5` version line, `g-wiki/README.md:5` current-state line, CHANGELOG `[2.5.0]` dateline dated at the cut · Task 21 release grep sweep (§D step 5 — grep the literals `2.4.1` / `unreleased` / status words across live surfaces, never a site list) · merge to `main` `--no-ff` + push · Task 22 tag `v2.5.0` + GitHub release with the CHANGELOG section as body (gh account has ADMIN on onlygian/G-Forge) · freeze. Publish-time carries from the comms plan §7: announcement block-order decision (developer, open); the four re-verify items (one is a dead row — no "same verdict" sentence exists in the rewritten §3c). M52 close-out at the release commit: `/g-align` + `/g-telemetry` close swarm, `/g-wiki` refresh (closes the g-wiki/usage.md count-twin advisory), `/g-patterns` after the swarm, milestone → ✅.
**Handoff at retro:** the F3 handoff's Next-up (condensed here; the verbatim text lives in `bf36fbe`'s ROADMAP block) — "**Session D — the release** (per the M52 entry, 'Then Session D as written'): read `g-docs/communication-plan-2.5.md` · `/g-doctor` release-hygiene precondition — resolve, or explicitly accept in the record, every finding before the bump · **developer decision first: does todo row 19 … gate the release, or ride to G-Proof R0?** · Task 20 version bump · Task 21 release grep sweep · merge to `main` `--no-ff` + push · Task 22 tag `v2.5.0` + GitHub release · freeze. Nudges due: `/g-trim` · `/g-align` · M52-close `/g-wiki`" — executed this session through the comms-plan read, the doctor run + acceptances, the row-19 decision (gates), the fix, and both gates; the bump-through-freeze tail is superseded by the Next up above.
**Key files touched:** commit-detect.sh, test-commit-detect.sh, CHANGELOG.md, todo.md, todo-done.md (+ gitignored: CLAUDE.md, .claude/hooks/lib/commit-detect.sh, reentry.md, last-trim, review-holds).
**Carry-over context:** Records (gitignored): wave `agent-output/wave-d/task-50-quote-aware-pad.md` + `task-50-fix-r1.md`; attestations `g-forge-dev-2026-08-31-d-r{1,2}.md`, `g-forge-dev-2026-08-31-d-fixtures-r{1,2}.md`; gate records `code-lead-2026-08-31-d-task50-r{1,2,3}.md`, `doc-reviewer-2026-08-31-d-task50-r{1,2,3,4}.md`; frozen diffs `diff-2026-08-31-d-task50*.patch` (r1–r6). Advisory carries: code r3's three minors (tests:190 completeness universal — durable close is a derive-the-list invariant, noted at code r2:447; two pad-frame wording items); doc r4's two warnings; plus the F3-era carries (code r7 "five reviewers" qualifiers, currency r5 warnings) and `/g-trim`'s doc-reviewer memory curation flag. `review-holds` 2 (code r1 + r2 this session — reset is `/g-telemetry`'s call at a stable profile). Observer gap persists: `bash tests/*.sh` runs unjournaled; this session's journal is agent-events + one commit.

## Journal basis
184 agent · 1 commit · 1 destructive · 1 session (events since this session opened at 10:49Z; suite runs unjournaled per the known observer gap)
