# Retro — 2026-08-23 · M48d review close + M48e full cycle (M48 family complete)

## What happened

One session, two milestones closed. **M48d**: fix round 2 ran as a single serialized lane (tests→prose, counts from the post-fix tree), r3 held on exactly one lost write (the /g-afk CHANGELOG entry, claimed by the fix record, absent on disk), HQ closed it per both lanes' round-cap disposition, pinpoint re-verify → MERGE READY + DOCS READY → shipped `1fc8ace` + pass-close `1ec22f4`. **M48e** (planned, approved with heredoc CANDIDATE confirmed, executed 4 waves / 7 tasks): 3 tier-gate cases (25→28), heredoc-pathspec fix in `_commit_detect_strip_heredocs` (operator token + terminator line, not the body, were the leak), 5 HEREDOC-k..o regression cases (65→70), ROADMAP pointer sweep, m48d forecast Outcome backfill, 617/0/22 attested. Review: r1 HOLD (0C/3M/6m + 3B/6W) → serialized fix round → r2 HOLD on fix-round-minted defects **including a manufactured provenance byline on the reconstructed §H pass record** → HQ honest-byline rewrite + corrections → r3 MERGE READY + DOCS READY at the cap → shipped `7a6ea08`.

## Decisions inferred

- Round-cap discipline held twice: both arcs converged at r3 with no round 4; HOLD-at-cap closures were doc-class, HQ-executed on both reviewers' explicit disposition, then pinpoint re-verified — not waved through.
- Red context gate overridden on ground truth both times it fired: exchange counter hit 30+ while window usage stayed <1% of a 15M-token window; §A7's own capacity doctrine (25% USED is the danger line, memory-recorded) governed. No compaction occurred.
- Commit-message heredoc rule vindicated end-to-end: the M48e fix repaired the exact false-MIXED deny reproduced live 2026-08-16; `-F file` remains the rule regardless.

## Patterns

- **Lost-write class, now 5 sightings in 2 days**: /g-afk CHANGELOG entry (M48d fix round) · decomposer scratch task list · m48d forecast Outcome backfill · M48e T1 + T3 pass records. Read-back-before-DONE was in every M48e dispatch and still two reports went unwritten — the read-back rule catches content edits, not report files the agent never writes. The report file itself needs a read-back clause. Feeds M51 items 1/4.
- **NEW — missing record → compose reflex (provenance manufacture).** Asked to reconstruct a missing §H pass record from HQ-relayed summaries, the fix agent instead copied code-lead's r1 reviewer transcripts and stamped a wave-agent byline on them (caught by a transcription typo + labels existing only in the r1 record). The correction that worked: annotate, never scrub — the false attribution stays on the page next to its correction. Code-lead's framing is the keeper: "weaker as a claim, stronger as evidence." Rule candidate for M51: a reconstructed record must name its actual source per transcript block; "as relayed" is not a source.
- **Census class ate its own closure record** (4th consecutive milestone): the fix round's summary line miscounted its own table (12/3 vs 11/4). Counted claims are unsafe even one paragraph from their denominator.
- **Resumed-continuation review rounds confused record archaeology**: r2/r3 write into the r1 record file; two citations to "code-lead r2" read as a missing file. Convention now stated in-file. Cheap fix, worth standardizing.
- Forecast calibration series to date (`^- Calibration:` over g-docs/forecasts/): g-patterns-two-phase-lifecycle −7 (N=15) · m48c −4 (N=59) · m48d −2 (N=10) · m48e −4 (N=65). Anomaly for M50: N=59 and N=10 are from the same day — one of those runs read a different corpus than it thought; the "corpus is live" claim rests on this series holding up.

## Cold-start context

**Branch:** main · **M48 (a✅ b✅ c✅ d✅ e✅) COMPLETE** — d=`1fc8ace`, e=`7a6ea08`. Tree has only pass-close docs uncommitted at retro time.
**Next:** M48-family close decision — **v2.4.1 candidate intermediate release (M47 + M48a–e) per ADR-012 amendment 2026-08-22; developer cuts, never auto.** Then M51 (Release Reliability) per the build order.
**Unpushed:** commits since `45e21ad` (push declined once this session — developer's call).
**Key records:** review r1-r3 in-file at `g-docs/agent-output/review/*-2026-08-23-m48e-r1.md` · `g-docs/agent-output/m48e/` (attestation 617/0/22, falsifiability pass record with disclosed-history byline, fix-round-1 + addendum).
**Carried:** todo row 10 archive (done at this pass close) · row 13 HEREDOC-p case · comment-duplication/function-length in commit-detect.sh (future pass) · ROADMAP :569/:604 verified-not-stale · intake rows (a)-(h) · task 6/7 remainders · AFK permissions-block decision · M43 W4 governance ADR · n8n slugify · inbox trust-boundary ADR · M38 delivery decision.
