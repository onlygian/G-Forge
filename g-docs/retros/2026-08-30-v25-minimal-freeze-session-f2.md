# Retro: v25-minimal-freeze-session-f2 — 2026-08-30

## What was done
- **M52 F2 audit read** — HQ on the session model (§A1 override) read the eight SURVIVES agents and the four record-seam skills whole (12 files) and wrote `g-docs/audits/2026-08-30-fable-f2-survives-agents-record-seam.md` before any dispatch: 4 Major (F2-1 ask-and-wait in dispatched agents · F2-2 `project-manager` self-contradiction + Superpowers path + inline pipeline · F2-3 reviewer `memory: project` carrying stale "verified clean" verdicts · F2-4 `/g-retro` handoff refresh self-copying) + 3 Minor (F2-5 stale `compact-state.md` · F2-6 repo-only facts / hardcoded `main` · F2-7 `doc-writer` README check); 5 out-of-slice carries → todo row 18.
- **Two live probes** — a dispatched `project-manager` reported `Agent, Read, Write, Edit` available and no user channel; a dispatched `test-writer` with no framework named inferred the bash convention from `tests/` and wrote the file instead of asking. Both quoted in the record.
- **Wave F2 (Tasks 29, 30, 31+32)** — three `claude-plugin-implementer` dispatches in parallel, all `DONE` first attempt, no stalls, no child dispatch: `agents/project-manager.md` rewired to `/g-plan` → `/g-execute` → `/g-review` with a `## Return format` block; the memory rule in the five `memory:` agents; the three ask-and-wait sites; `doc-reviewer`/`doc-writer` edits; `g-retro` Step 3/5b, `g-resume` Step 1, `g-doc-review` `:28`/`:44`, `g-adr:227`; three guard groups in `tests/test-review-severity.sh`. HQ pre-read every diff; one inline fix (a typed `SKILL.md:211` line ref → "`/g-review` Step 4"). Carrier sweep across README/wiki/g-docs/rules/profiles/templates/skills/agents/tests: no survivors on any shipped surface (the one in-repo carrier of the ADR-013 relative link, `tests/README.md:7`, resolves and ships to no consumer — it stays; the audit record's self-review section holds the grep); `main...HEAD` twins in `code-lead.md:38`, `pr-writer.md:13`, `g-docs/agents.md:200` added to todo 18.
- **Task 33 attestation** (`g-forge-dev`, record `g-docs/agent-output/review/g-forge-dev-2026-08-30-f2-r1.md`, gitignored) — runner `Grand total: 732 passed, 0 failed across 24 suites`, delegate's independent sum agrees; `test-review-severity.sh: Results: 21 passed, 0 failed`. Falsifiability probes on a scratch copy, run from a script file: baseline GREEN 21/0; neuter doc-reviewer's memory rule → RED 20/1 naming doc-reviewer; re-add `Only proceed after they answer` to test-writer → RED 20/1 naming the literal; break `## Return format` in project-manager → RED 20/1; add a `memory: project` agent with no rule → RED 21/1 naming the new file (enumeration is disk-derived). Markers not yet added to the test file.
- **Task 34 (HQ)** — seven `CHANGELOG.md` `[2.5.0]` Fixed entries; todo row 18.
- **Session closed early on a developer directive** — redo the F2 audit on Fable 5 Extra in a fresh session, tree kept as is (developer chose "keep everything in the tree" over stashing the wave). The wave and `CHANGELOG.md` are on disk, ungated and uncommitted; this pass-close commits only the `g-docs` surface.

## Decisions made
- **Redo the F2 audit on a stronger model tier before gating the wave** (developer, at close). Evidence: the directive in chat; no commit of the wave this session.
- **Tree kept as is for the redo** (developer, at close) — the redo reads the slice with the F2 fixes in place; `git show HEAD:<path>` gives the pre-wave text.
- **F2-3 rule applied to all five `memory:` agents, three of them out of slice** (HQ, amendment-4 "adopter-facing" arm, disclosed in the record's carry list 5).

## Patterns
### Worked well
- Record first, probe every finding, one wave, separate attestation — same shape as F1; three parallel implementer dispatches with disjoint file sets, all first-attempt `DONE` (journal: three `claude-plugin-implementer` start/stop pairs, no re-dispatch).
- Live dispatch probes settled two contract questions cheaply (PM tools; test-writer's no-framework path) that reading alone could only argue.
- HQ pre-read caught a typed line ref before the gate (the todo 15 R2-2 class).
- Every new guard proved it can fail before anyone trusted it, and the disk-derived enumeration was probed separately from the rule assertion.
### Avoid / do differently
- **F2-4's fix ordered the derivation wrong for this repo, and this very close showed it:** "lead open `g-docs/todo.md` row" would have produced todo row 5 (a carry) as Next up, while the actual next action was the developer's directive. On this repo the plan/milestone drives and todo rows are carries — the redo should re-examine the derivation order (directive/handoff intent → plan wave → milestone task → todo row).
- **The observer flagged HQ's own `cat > .claude/reentry.md <<'EOF'` as `destructive`** (journal 17:48Z), and a second HQ heredoc failed to parse at all — the §C rule (records via the Write tool, never heredocs) applies to HQ too.
- **An attestation dispatched last is an attestation that may not land before the session ends** — it arrived while the retro was being written. Dispatch it as soon as the test file exists, before the carrier sweep and CHANGELOG work.
- **A count in the record is a dated citation of a runner line, not a claim** — label it with its source run/commit or the doc gate reads it as volatile state.

## Cold-start context
**Branch:** `chore/v2.5-minimal-freeze` — ahead of `origin/main` by `59c7c63` · `7beeabf` · `9dd49c4` · `7208967` · `2633981` · `6592bf2` · `a1a1c2b` · `5ed9839` · `4e54ef1` · this pass-close commit; never pushed, no upstream. **Tree dirty after this commit:** the Wave F2 edits (`agents/` ×9, `skills/` ×4, `tests/test-review-severity.sh`) and `CHANGELOG.md` — ungated, uncommitted, by developer decision.
**Active milestone:** M52 — v2.5 Minimal Freeze 🔄 In progress (A, B, C, F1 closed; F2 wave landed on disk pending the redo audit; F3, then Session D release owed).
**Next up:** Redo the F2 audit on Fable 5 Extra in a fresh session — `/g-resume`, read the same 12-file slice (fixes present on disk; `git diff`/`git show HEAD:<path>` for the pre-wave text), write `g-docs/audits/2026-08-30-fable-f2-redo-….md`, diff against the F2 record, decide per finding (keep / extend / revert the landed fix); then add the falsifiability markers to the three new guards (probes already RED, see Task 33 above), `/g-review` + `/g-doc-review` on the whole wave, commit, retro, handoff. Derived from the developer's directive at close (the todo lead row is a carry, not the next action).
**Handoff at retro:** · **F2 — Fable audit cycle 2** (`/g-resume` first, fresh session): slice = SURVIVES agents (`architecture-enforcer`, `doc-reviewer`, `spec-writer`, `test-writer`, `debugger`, `error-detective`, `doc-writer`, `project-manager`) + the record seam (`g-resume`, `g-retro`, `g-adr`, `g-doc-review`). Same shape as F1: audit record first, probe every finding, filter, one wave under the caps, separate `g-forge-dev` attestation, `/g-review` + `/g-doc-review`, commit, retro, handoff. Then F3 (remaining SURVIVES skills + open ledger todo 5, 6, 9, 14, 15, 16), then Session D (read `communication-plan-2.5.md` · todo 15 F-set · Task 20 bump · Task 21 sweep · merge `main` `--no-ff` + push · Task 22 tag + release · freeze). Nudges due: `/g-trim` (last 2026-08-18), `/g-align`, M52-close `/g-wiki`.
**Key files touched:** architecture-enforcer.md, code-reviewer.md, dependency-auditor.md, doc-reviewer.md, doc-writer.md, error-detective.md, project-manager.md, security-auditor.md, test-writer.md, SKILL.md (g-adr, g-doc-review, g-resume, g-retro), test-review-severity.sh, CHANGELOG.md, todo.md, ROADMAP.md, 2026-08-30-fable-f2-survives-agents-record-seam.md
**Carry-over context:** Wave records `g-docs/agent-output/wave-f2/task-{29,30,31-32}*.md` and attestation `g-docs/agent-output/review/g-forge-dev-2026-08-30-f2-r1.md` (gitignored); plan Session F2 block (`g-docs/plans/`, gitignored). Todo 18 holds the five F3 carries (nested `Agent` grant is live → `review-orchestrator:11` wrong; `workflow-checkpoint.sh:389/:394` false "verify ADR" nudge; `main...HEAD` twins; missing return blocks; three memory agents un-audited). Gitignored `CLAUDE.md:23` was rewritten from the attested `Results:` line during this close (the r1 doc gate caught the retro still scheduling it) — nothing owed there.

## Journal basis
32 agent · 1 session · 1 destructive (events since this session opened 17:46Z, derived from `.claude/journal/2026-08-30.jsonl` at retro time)
