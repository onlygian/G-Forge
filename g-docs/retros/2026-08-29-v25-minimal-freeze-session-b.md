# Retro: v25-minimal-freeze-session-b — 2026-08-29

## What was done
- **Wave 3b — the r1 doc-gate fix round, all six blockers, HQ inline (no delegates).** B1 five stale-scope surfaces (`g-wiki/README.md`, `project_brief.md` ×2, `M41.md` archive-moved to `g-docs/archive/milestones/` with a not-plannable stamp, `ADR-010`); B2+W5 ROADMAP Version Plan gloss and nine-milestone chain retired; B3 comms plan heading/tense; B4 ADR-012 retitled, header amendment line added, Decision/Constraints/Alternatives/Consequences/Risk all struck-and-stamped; B5 README:42 shipped tag restored; B6 `skills/g-review/SKILL.md` solo-review copy fix.
- **Pre-gate literal sweep caught an unscheduled stale fact:** `g-docs/g-proof-roadmap.md` has been tracked since 2026-08-21 (`45e21ad`) while five surfaces still called it local-only/gitignored/hand-carry. Corrected at all five.
- **G-Sharp adopter field report read and reproduced against source** (`g-docs/field-reports/2026-08-28-g-sharp-telemetry.md`). Both defects confirmed on this repo: 0/34 retros carry the spec's metric-1/-8 tokens; the `review-holds` latch stood at 34 against 30 `feat:` commits.
- **Task 23 (developer-approved scope addition):** `review-holds` reset removed from `/g-telemetry`, `/g-review` now owns increment + decrement floored at 0 gated on Step 4b evidence; `/g-telemetry` forbids semantic substitution and reports a measurement vacuum below `computable < 5` (raised from 3, agreeing across spec and skill); spec reads the `Avoid / do differently` heading at the level `/g-retro` ships (`###`); `/g-telemetry` agent roster made derived from `agents/*.md` (was hand-typed 19 with a 17-row table). New `tests/test-telemetry-contract.sh` (23 assertions); `EXPECTED_SUITE_COUNT` 22→23; `tests/README.md` + `CLAUDE.md` re-attested at **645 across 23 suites**. `.claude/review-holds` reset 34→0 with evidence.
- **Overrun rule invoked:** Tasks 15 and 18 cut to pay for Task 23; recorded in plan and ROADMAP.
- **Doc gate rounds 2 and 3** (`doc-reviewer-2026-08-29-...-r1.md`, `-r2.md`): DOCS HOLD 5B/13W, then 3B/12W. All eight blockers fixed inline, plus W1/W4/W5/W10/W11/W12 and five r3 warnings. Task 15's rule-text half un-cut at the r2 gate by developer decision (three sites now read "~25% of the window used"); the `25%` sweep stays cut.
- **`agents/code-lead.md` three `AXES:` clauses stamped inert** — the verdict contract was conditioned on an artifact nothing in the pipeline emits.
- **Backlog entry written:** context-budget gauge defect (exchange-count proxy fired at 40% used; model cannot read `/context`; session-budget counter misread as window fill). G-Proof R0 candidate.
- **Tier 1 attested five times** across the session; final: 645 passed, 0 failed, 23 suites, hand-summed = runner total.

## Decisions made
- **Reviewer delegates only, implementers none.** After Session A's 4-resume yield rate, every fix this session was applied by HQ inline; the only dispatches were two `doc-reviewer` gate rounds, both of which wrote their record first attempt (journal: 2 start/stop pairs, 41 agent events total, 0 implementer dispatches).
- **Task 23 accepted into 2.5 under ADR-012 amendment 4's filter** — the latch fix passes both limbs (rebuild map `:53`: adaptive profile *survives*), the vacuum fix passes the adopter-facing limb; §1a/§1b/§3 declined as hand-counting improvements the map marks dying. Paid for by cutting Tasks 15/18 (ROADMAP M52 scope amendment, 2026-08-28).
- **Task 15 partially un-cut at the r2 gate (developer).** The gate raised the §A7 rule text to BLOCKING once the cut removed its owner; the three known sites were restored, the open-ended sweep stays cut (ROADMAP overrun note; plan Task 15 row).
- **Doc-gate omission on `review-holds` resolved by stating scope, not adding mechanism** — the counter is code-gate HOLDs only, by design, recorded in the spec.
- **Line-number cites in ADR-012 re-anchored to sections** after going stale three times inside one pass.
- **Reset at 40% window used** rather than run doc round 4 / the code gate in a residue-heavy window (`/context` 397.5k/1M).

## Patterns
### Worked well
- **The falsifiability probe earned its keep on first use.** `tests/test-telemetry-contract.sh` reported 23/0 twice; the probe found 3 of 5 guards green-while-broken (a `live()` shell function invisible inside `bash -c`; a `[^.]` class unable to cross the dot in `.claude/review-holds`). Fixed, re-probed, all five RED against a GREEN baseline. Both traps documented as NOTE comments at the assertion sites.
- **Pre-gate literal sweep caught one unscheduled stale fact per round** — `g-proof-roadmap` tracking status, then `g-telemetry:131`'s hand-typed 19 six lines below the roster just made derived.
- **Reviewer records were written first attempt, both rounds** — the "write your record file first" instruction in the dispatch prompt held (forecast scenario 2, mitigation-held).
- **Every Tier 1 total was hand-summed from the per-suite table before acceptance**, five runs; all matched.
- **Adopter field report reproduced against source before scope was decided** — both defects measured on this repo, one found worse here than on the reporter's project.

### Avoid / do differently
- **Fixing one carrier while its twin survives in the same file — three rounds running.** README:42 fixed, comms-plan:66 (the verbatim §3c source scheduled for release-notes publication) survived. `g-execute:156`'s conditional flipped to "25% used", the closing "Above the floor, proceed." left reading the old gauge — stop and proceed for the same state. Four `](decisions/` links re-depthed in M41.md, four more lifted verbatim into the archive file missed. Every r2/r3 blocker was a twin of an r1/r2 fix. Next time: when a fix lands, grep the *file* for the fact's other forms before moving on, not just the repo for the literal.
- **The done-condition instrument was a phrase-grep; the bug carried the direction without the phrase.** Task 15's absence-grep for `remaining capacity` passed over "Above the floor, proceed." G-RULES §H's instrument-claim rule names this exactly. Done condition rewritten to a direction instrument; the general rule is that a fix to a *qualifier* needs an instrument that reads the qualifier.
- **HQ minted two false facts inside fixes about false facts.** "Its contract does not mention one" as the B6 evidence — `code-lead.md` mentions the panel three times. "167%" in five files — `fix_after_feat` counted all `fix:` commits (16) not the windowed formula value (7); real figure 137%. Both caught by the gate. Next time: a number or a negative claim written into a fix gets the same verification as the defect it replaces.
- **Session-budget counter read as window fill.** The `total_tokens` remaining figure (~1% of 15M consumed) sat adjacent to the real gauge and was read as reassurance while the window went 28%→40%. The §A7 banner fired at exchange 15 with the window already at 40%. Backlog entry written; until fixed, `/context` must be run by the developer and pasted back on Remote Control.
- **Round numbering scoped to date+slug.** Three rounds of one target minted `2026-08-28-r1`, `2026-08-29-r1`, `2026-08-29-r2` — a review spanning midnight gets two `r1`s. Not fixed; noted.

## Cold-start context
**Branch:** `chore/v2.5-minimal-freeze` off main `d724265`
**Active milestone:** M52 — v2.5 Minimal Freeze 🔄 (Session B in progress; Session C Saturday PM; Session D Sunday 2026-08-30 release)
**Next up:** · **Doc gate round 4** — `/g-doc-review` with the closure sweep for r3's three blockers (`ROADMAP.md` Wave 7 parenthetical, `g-execute` "Under it — less than ~25% used — proceed.", four archive `../decisions/` links) plus five r3 warnings closed (M41 archive date unified on 08-28; ADR-012 `:36` cites re-anchored; `ROADMAP` test cite `:394`→`:462`; `telemetry-metrics.md:6` sub-counts omitted; plan done-condition instrument). Carried unclosed by choice: W2 comms §7, W3 §3c/README present tense (Session C), W6 handoff (this rewrite closes it), W7 retro figures, W8 forecast placeholders, W9 ship vehicle, W13 CHANGELOG:11. · **Then `/g-review`** — first code-gate run of the pass; surface: `skills/g-review`, `skills/g-telemetry`, `skills/g-execute`, `agents/code-lead.md`, `rules/g-rules/A-session.md`, `tests/test-run-all.sh`, `tests/test-telemetry-contract.sh`. Reuse the attested 645/645 run. · **Then commit** — changeset is **mixed** (both sentinels). Commit message draft at `<scratchpad>/commit-msg-session-a.txt` is stale (Session A only) — rewrite. · Then Session B Waves 4–5 (Tasks 6, 6b, 7, 8, 9, 10, 10b, 11, 12, 13).
**Key files touched:** README.md, ROADMAP.md, project_brief.md, communication-plan-2.5.md, telemetry-metrics.md, g-proof-roadmap.md, 010-full-rebuild-on-current-platform.md, 012-g-forge-2.5-final-release-scope.md, archive/README.md, archive/milestones/M41.md (moved), archive/roadmap-dropped-2026-08-28.md, g-wiki/README.md, code-lead.md, A-session.md, g-execute/SKILL.md, g-review/SKILL.md, g-telemetry/SKILL.md, test-run-all.sh, test-telemetry-contract.sh (new), tests/README.md, CLAUDE.md (local), plans/v25-minimal-freeze.md (local), forecasts/v25-minimal-freeze.md
**Carry-over context:** Tree dirty, **23 paths** uncommitted (18 modified, 1 rename, 4 untracked incl. the field report and the new suite), nothing committed on the branch. Both sentinels absent. Doc gate HOLD at r3 with all blockers fixed on disk and unverified by a round; code gate never run. Tier 1 attested green after the r3 fixes: 645/645, 23 suites, hand-summed. `.claude/review-holds` = 0 (reset with evidence; the old policy's 34 was a lifetime total). `hooks/workflow-checkpoint.sh:271,:279` still read the retired "remaining capacity" direction — Task 14, Session C; the amber banner currently contradicts the rule it enforces. Session B closed at **40% window used** (§A7 floor 25%), after the exchange-count proxy fired at 15. The `.claude/rules/` installed copy of A-session.md still carries the old wording until `/g-update` realigns it. LOCAL STATE: voice gian · telemetry cautious · last-trim 2026-08-18.

## Journal basis
2026-08-29: 43 events — 2 session (startup, resume) · 41 agent (2 doc-reviewer start/stop pairs; the rest internal stops). 0 commit · 0 test (suite runs were HQ Bash, not journaled) · 0 revert · 0 destructive. Prior day 2026-08-28 read for Session A continuity.
