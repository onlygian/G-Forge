# ADR-012: What G-Forge 2.5 ships

> **Title changed 2026-08-28.** This ADR was filed as *"G-Forge 2.5 ships the full announced scope"*. Amendment 4 **reversed that decision** — 2.5 is a minimal freeze — so the original title now names the opposite of what the ADR decides. The ID and the file path are unchanged; every existing citation still resolves.

**Date:** 2026-08-10
**Status:** Accepted
**Amended:** 2026-08-14 — M49 (Devil's-Advocate Agent, internal adversarial pattern review) folded into the 2.5 scope; see "Amendment — 2026-08-14" below.
**Amended:** 2026-08-17 — M50 (Eval-Chain Integrity) folded into the 2.5 scope, sequenced **third** (after M48, before M45); see "Amendment — 2026-08-17" below.
**Amended:** 2026-08-20 — M51 (Release Reliability / M45-lite) inserted **third** in the build order, absorbing M45 (Review Pipeline Rework); M50 moves to fourth. See "Amendment — 2026-08-20" below.
**Amended:** 2026-08-22 — intermediate patch/minor releases cut at milestone-family closes; 2.5 scope and freeze identity unchanged; see "Amendment — 2026-08-22" below.
**Amended:** 2026-08-24 — rendering rule for the README's "What's coming in 2.5" copy at intermediate cuts: shipped bullets are marked in place; see "Amendment — 2026-08-24" below.
**Amended:** 2026-08-28 — **the Decision itself is reversed.** 2.5 becomes a minimal freeze: it ships only what survives the G-Proof rebuild or is an adopter-facing bug. The full-announced-scope decision, its "approved copy is a commitment" constraint, the nine-milestone build order and the chosen alternative (4) are all superseded; M52 replaces them. **Read "Amendment — 2026-08-28: minimal freeze" below before anything under `## Decision`.**
**Reversibility:** two-way door, narrowing — **and it was walked through on 2026-08-28** (amendment 4). The recorded cost was exactly right: cutting announced items meant editing published copy, and that was a developer decision, taken. The hedge held — the §3c copy sat on the README from 2026-08-10 under "What's coming in 2.5" and bound no reader, so the narrowing cost a rewrite rather than a broken promise. The heading is now present-tense ("What's in 2.5") ahead of release, and **the door is closed: 2.5's scope is the minimal freeze.**
**Context:** G-Forge plugin source repo. Decision made by the developer in session, 2026-08-10 — this ADR captures it; the deliberation was the session's intake + triage, not a subagent pass.

## Amendment — 2026-08-28: minimal freeze

Developer decision (session record, 2026-08-28): **the 2026-08-10 full-scope decision is amended — 2.5 ships the subset that survives the G-Proof rebuild or is an adopter-facing bug fix.** Alternative 1 under `## Alternatives Considered` below ("Minimal freeze — M41 + M47 + M48 only, fork everything else") is reopened on new evidence and chosen **in kind, not verbatim**: the minimal-freeze *shape* is adopted, but M41 is dropped rather than kept (v2.4.1 proved the hand-cut works), and slimmed M51/M40 items are added — see "In 2.5 after this amendment" below for the actual contents. The constraint that drove the full-scope decision ("The approved copy is a commitment") is dissolved: the sole adopter accepted a thinner 2.5, and no outside reader the published §3c copy binds. The README copy follows reality — "README needs to be actual and true."

**The filter:** each milestone is evaluated against the rebuild map (`g-docs/audits/2026-07-rebuild-map.md`) verdict column — what survives G-Proof day-one or is adopter-facing. Milestones whose map row marks DIES/TRANSFORMS are deleted from 2.5 scope because the fork re-builds them anyway; shipping them here wastes effort on code that lasts one day. Evidence base: `2026-07-rebuild-map.md` rows review-orchestrator/code-reviewer/security-auditor/wave-planner (four agents marked DIES, §21); the review panel is a solo bottleneck since May (`g-docs/retros/2026-08-28-patterns-resolve-27.md`, Cold-start context); shipped milestones M47/M48 required 3–5 code rounds each, and the 2026-08-27 prose pass on the patterns-resolve report ran 7 gate rounds with most Majors being stale carriers from the prior round (retro journal section 2026-08-27).

**In 2.5 after this amendment:** M47 ✅, M48 family ✅ (already shipped in v2.4.1); **M52 — v2.5 Minimal Freeze** (new release milestone) absorbing: M51's items 3 (Tier 1 CI test, not a gate) + 4 narrowed to the two `output_file` dispatch sites + README all-agents-write correction + 5 (A7 re-sync) + 6 (S-fixes) + 8 (forecast relabel) + 9 (trivial-task light-tier routing); M40 Wave 1 only (REFERENCE classifier arm + taxonomy row + g-init line + tests). The release is hand-cut per G-RULES §D as v2.4.1 was on 2026-08-24 — the evidence that the hand-walk works now exists, which it did not when alternative 3 was rejected.

**Dropped (to g-docs/archive/roadmap-dropped-2026-08-28.md, committed, but each a G-Proof R0 candidate to decide):** M38 `/g-report` (TRANSFORMS to GitHub issues in the announcement), M41 release machinery (one ship vehicle survives for M52 hand-cut), M43 `/g-settings` (dotfile layer TRANSFORMS/DIES, G-Proof rebuilds it), M50 (TRANSFORMS, agents die), M49 (new agent in dying layer), M51 items 1 (panel wiring — DIES) + 7 (estimator deletion — blast-radius case for G-Proof) + 10 (cosmetic), M40 Waves 2–3 (two-thirds of the milestone deferred to fork). All entries retain their milestones' design rationale — not deleted, archived to the g-docs record for the fork decision.

**Consequences:** (1) The announcement copy (`README.md` §3a/§3c, `communication-plan-2.5.md` §3) is rewritten before the 2026-08-30 cut (previously 2026-09-10 estimate) to describe what ships, not what was planned: `/g-report` → GitHub issues, `/g-settings` removed, reference material + A7 re-sync + S-fixes framed as "maintenance correctness" not "new capability". (2) The Version Plan's nine-milestone build order (`ROADMAP.md`, the `## Version Plan` block — cited by section, not line: this file's line cites have gone stale twice, and the block moves whenever anything above it is edited) is retired in favour of M52's four implementation sessions (`g-docs/plans/v25-minimal-freeze.md`). (3) ADR-010 is unchanged — v2.5 is still the last release and the fork happens at the same tree boundary; the contents are thinner, the schedule is shorter, and the rationale reflects adopter acceptance.

**Surface set updated for this amendment** — this ADR (header + this section + scope table below, amended in place with dropped rows struck) · `ROADMAP.md`'s Version Plan (remove the nine-milestone build order, link to the four-session plan instead) · `communication-plan-2.5.md` (§3a copy edit: `/g-report` → GitHub issues; §3c removed bullets for M38/M43/M50/M49/M51-7) · `README.md` (M38/M43/M50/M49/M51-7 bullets removed from the "What's coming in 2.5" section, re-rendered to present tense) · `g-docs/milestones/M41.md` → archived — **done 2026-08-28**, moved to `g-docs/archive/milestones/M41.md` with a dated archive stamp (no M41 in 2.5, hand-cut M52 replaces it) · `CHANGELOG.md` — **cite corrected 2026-08-28:** `:11` is the `/g-patterns` `[Unreleased]` bullet, not a 2.5 announcement; the actual carrier is inside the released `## [2.4.1]` section, which repo convention keeps append-only, so the v2.5.0 announcement is written fresh at the Session C `[2.5.0]` entry rather than edited here · `g-docs/milestones/M47.md`, `M48.md` version stamps (M52 has no milestone file — its ROADMAP entry carries the stamp) · `project_brief.md` — **cite corrected 2026-08-28, re-anchored 2026-08-29:** the Naming/version-strategy row is unchanged by this amendment; the live scope carriers are the **v2.5 roadmap row** in the milestone-arc table and the **ADR-010 "Full rebuild + freeze/fork" standing-state bullet**, both updated. Named by content rather than line: the line numbers moved between the two rounds of this very pass · `g-docs/archive/roadmap-dropped-2026-08-28.md` (new, all dropped milestones with their entries intact for fork reference). No typed site list is authoritative — this one included: the complete set is found only by sweeping, paying attention to version numbers in context (M52 references, M41 removals) and both the bare milestone token AND sequencing words.

## Amendment — 2026-08-24: "What's coming in 2.5" rendering at intermediate cuts

Developer decision (session record, 2026-08-24, at the v2.4.1 doc gate): when an intermediate cut ships an item the README's `### What's coming in 2.5` copy still announces as future, the affected bullet is **marked in place** with a short shipped tag (e.g. *"(Already shipped early — in v2.4.1.)"*) — the section keeps its approved shape and the promise list stays visible; bullets are never silently removed or moved to a separate block. A bullet that mixes shipped and unshipped claims gets the tag scoped to the shipped sentence only. This resolves the gap the 2026-08-22 amendment left open (it introduced intermediate cuts without addressing this copy), first hit at the v2.4.1 cut (`README.md:44,:50`); the rule applies at every subsequent family close. At the 2.5.0 release the heading goes present-tense per the original decision and the tags dissolve with it.

## Amendment — 2026-08-22: intermediate releases on the road to 2.5

Developer directive (session record, 2026-08-22): the 2.5 scope and the freeze-release identity are unchanged, but the road to it now cuts **intermediate patch/minor releases at milestone-family closes** — first candidate v2.4.1 at the M48-family close (M47 + M48a–e). Rationale: the project's own testing model is release-and-dogfood, and a monolithic 2.5 starves the field-validation loop for months — everything hardened since v2.4.0 was un-dogfoodable outside this repo. v2.5.0 remains the final G-Forge label — **M41 cuts it** (sequenced last in the build order, `g-docs/ROADMAP.md's M45 entry`); **M51 defines the release condition** it must meet ("a reliable and very usable harness," not a doc patch — `g-docs/ROADMAP.md's `## Version Plan` block`) — cumulatively containing everything this ADR promises; the intermediate cuts change delivery cadence, not scope. Standard release flow (§D) applies to each cut: both manifests, CHANGELOG, tag, GitHub release. *(Line cites `:451`/`:622` retired 2026-08-29 — both had gone stale as the file grew; anchored to sections, matching this ADR's own "no typed site list is authoritative" caveat.)*

**Surface set updated for this amendment** — this ADR (header + this section + the "one release vehicle" claim, amended in place at what is now `:70` below) · `CHANGELOG.md:11` · `g-docs/ROADMAP.md`'s `## Version Plan` block · M47's and M48's `**Version:**` stamps. No typed site list is authoritative — this one included: the complete set is found only by sweeping. *(Line cites `:667`/`:517`/`:547` retired 2026-08-29 — all three had gone stale as the file grew; anchored to sections and headings instead, which is what this ADR's own caveat implies.)*

## Amendment — 2026-08-20

**M51 (Release Reliability, M45-lite) is added to the v2.5 scope and absorbs M45 (Review Pipeline Rework)**, decided by the developer 2026-08-20 — the v2.5 release-condition scope change. Evidence base: two external 2026-08-20 audit reports (a gaps-and-fixes report enumerating A1–A10/B1–B12, and a benchmark pilot report); every acted-on claim from those reports was re-verified against the current tree at intake (code-lead holds no Agent grant; the jq rc-only check at `hooks/agent-lifecycle.sh:89-91` is live; `g-docs/g-proof-roadmap.md` is gitignored at `.gitignore:67`).

Sequenced **third** in the build order — after the M48 family, before M50 — because the review panel M51 wires is exactly the machine M50's own review-pipeline measurements need to already be pointed at; measuring it before M51 rewires it would measure the wrong machine. M50 moves from third to **fourth**.

**Why M51 absorbs M45 rather than the two running side by side.** The review panel has been solo-review since `daf15e3` removed `code-lead`'s Agent grant, with `51e5220` re-adding AXES language without restoring the grant — a wiring gap, not a design gap. The developer's decision (2026-08-20) is to wire the panel directly rather than doc-fix around the hole: `review-orchestrator` dispatched from `/g-review` at depth 0. This executes M45's design-ADR core as "M45-lite" — the reviewer record-write question (scoped-Write grants vs HQ-writes-records) is settled by directive (the doc-reviewer/task-decomposer scoped-Write precedent), not deferred to a separate design ADR, and review scope is fixed as diff + blast radius rather than left open. M45's own entry is retained as the design record (`Status: 🔀 Folded into M51`) rather than deleted.

The scope table below replaces the M45 row with an M51 row — nine rows remain (M45 is folded, not a tenth addition), so the two derived milestone counts (Consequences → Harder, and Risks) are unchanged by this amendment.

**Surface set updated for this amendment** — extends the 2026-08-17 list for M51, and corrects that list's own claim about `CHANGELOG.md:11`: this ADR (header + this section + scope table) · `ROADMAP.md`'s Version Plan build-order line, M41's `Depends on:` precedence list, M50's `Depends on:`/`Feeds:` lines, and M45's `Status:`/`Depends on:` fold note · `project_brief.md`'s v2.5 roadmap row and standing-state bullet · `README.md`'s v2.5.0 milestone list · `g-wiki/README.md`'s build-order line · `g-docs/milestones/M41.md`'s precedence list · `CHANGELOG.md:11`'s `[Unreleased]` announcement line, which the 2026-08-17 amendment recorded as not enumerating the milestone order — that record was wrong (it does, and was stale on both the M50 and M45→M51 folds); corrected here · `g-docs/todo.md` rows citing the folded milestone · every `ROADMAP.md` line naming the folded milestone as a live requirer or sequencing anchor. No typed site list is authoritative — this one included: the complete set is found only by sweeping, and the sweep needs BOTH the bare milestone token AND the ordinal/sequence words ("third", "fourth", "sequenced", "pinned", "before M", "after M" — examples, not a complete set), because a sequencing claim can name no milestone token at all.

## Amendment — 2026-08-17

**M50 (Eval-Chain Integrity) is folded into the v2.5 scope**, decided by the developer in session at the `/g-intake` triage that followed the 2026-08-17 `/g-telemetry` run. Sequenced **after M48, before M45** in the build order — M45's design ADR needs M50's agent contract map as an input, so ordering them the other way makes M45 guess. *(Superseded 2026-08-20 — M51 now sequenced third, absorbing M45; M50 moved to fourth. See "Amendment — 2026-08-20" above.)* The scope table below gains a **ninth** row for M50; the two derived milestone counts (Consequences → Harder, and Risks) go from eight to nine accordingly. M45's `Depends on:` moves from *third* to *fourth*. Nothing else in this ADR's decision or constraints changes.

**Why it is in 2.5 rather than the fork.** A 2.5/G-Proof split was proposed — fix the instrument *definitions* in 2.5, defer the *rewiring* to the rebuild, on the reasoning that ADR-010 replaces the current agent/hook layer anyway. The developer overrode it: **2.5 is a maintained freeze with real users, so it must do what it says at the best of its capabilities**, and "it gets rebuilt later" is not a reason to ship a release whose self-governance instruments do not measure what they claim. The override is recorded because it inverts the ADR-010 cost argument, and a future reader will otherwise re-derive the split and reach the wrong answer.

**What triggered it.** The `/g-telemetry` run of 2026-08-17 returned a clean-looking `cautious` profile while four of its own gauges were structurally incapable of registering the failures this project keeps recording — most starkly, coverage scored `code-reviewer` as `never` on a day the journal records it starting and stopping (`g-forge:code-reviewer start a8e145c2`, 2026-08-16). A fifth defect is user-facing: `g-docs/telemetry-metrics.md`, the skill's declared authoritative spec, **has no ship vehicle at all** — no skill creates it, so `/g-telemetry` reads a missing file on every consumer project.

**Surface set updated for this amendment** — the same list the 2026-08-14 amendment established as complete: this ADR (header + this section + scope table + both derived counts) · `ROADMAP.md`'s Version Plan build-order line, M41's `Depends on:` precedence list, and M45's `Depends on:` sequence position · `project_brief.md`'s v2.5 roadmap row · `README.md`'s v2.5.0 milestone list. `CHANGELOG.md:11` and the local-only `g-proof-roadmap.md:255` were **checked and require no edit** — neither enumerates the milestone list; they were in the 2026-08-14 set for unrelated reasons (freeze announcement, fork boundary). Recording the check so a future reader does not read their absence as an oversight. *(Superseded 2026-08-20 — the `CHANGELOG.md:11` half of that check was wrong: it does enumerate the order and was stale on two folds; see the 2026-08-20 amendment's surface-set correction.)*

## Amendment — 2026-08-14

**M49 (Devil's-Advocate Agent, internal adversarial pattern review) is folded into the v2.5 scope**, decided by the developer in session at the `/g-patterns` lifecycle intake. Sequenced **after M43, before M41** in the build order — the release-cutting milestone still runs last. The scope table below (originally seven rows + `shipped`) gains an eighth row for M49; the table and the two derived milestone counts below (Consequences → Harder, and Risks) are updated to eight accordingly — nothing else in this ADR's decision or constraints changes. `ROADMAP.md`'s Version Plan build-order line (`:570`) and M41's `Depends on:` precedence list (`:452`), `project_brief.md:38`, `README.md:766`, `CHANGELOG.md:11`, and the local-only `g-proof-roadmap.md:255` stamp all record the same amendment — this is the complete surface set for any future 2.5 scope change. This is the ADR-side entry the M49 review finding (MAJ-1, `code-lead-2026-08-14-patterns.md`) required.

## Context

ADR-010 set the delivery shape (v2.5 ships from this repo, the repo freezes, the tree forks into G-Proof, which ships as 1.0) but left the 2.5 contents open — the roadmap recorded "only M41 is a candidate" while the approved 2026-07-28 communication plan (`g-docs/communication-plan-2.5.md` §3c) promised seven concrete items, four of which the roadmap still assigned to v2.6.0–v2.15.0. The 2026-08-10 intake added two more milestones (M47 decomposer + calibration, M48 review hardening — both seeded by this repo's own nine-round fix-loop and the keyline field report, `g-docs/field-reports/2026-08-10-keyline-francesco.md`). The scope had to be settled in the record before anything else could be sequenced honestly.

## Decision

~~**G-Forge 2.5 ships everything the approved copy promises.**~~ *(Superseded 2026-08-28 — reversed by amendment 4: 2.5 ships only what survives the G-Proof rebuild or is an adopter-facing bug. See "Amendment — 2026-08-28: minimal freeze" above; that section, not this one, states what 2.5 contains.)* The 2.5 scope as decided 2026-08-10 was:

| In 2.5 | What it is |
|---|---|
| M47 | Planning-Pipeline Honesty (decomposer sizing + seam return, forecast calibration, review-chain cost term) |
| M48 | Review-Pipeline Hardening (grep-sweep required step, round-3 consolidation, volatile-fact heuristic, falsifiability rule) |
| M51 | 🔀 Absorbed into M52, slimmed (2026-08-28) — ~~Release Reliability / M45-lite (review panel wiring, gate execution coverage, CI, A6 grants, A7 re-sync, S-fixes, context-estimator deletion) — **added 2026-08-20**, absorbs M45 (Review Pipeline Rework, folded — retained as design record)~~ **Slimmed 2026-08-28** — items 3 (CI test) + 4 narrowed + 5/6/8/9 retained; items 1 (panel DIES) + 7 (estimator DIES) + 10 (cosmetic) dropped. See Amendment 2026-08-28. |
| M50 | 🗄 Dropped (2026-08-28) — ~~Eval-Chain Integrity (telemetry/coverage measure ground truth, agent contract map, firing audit) — **folded in 2026-08-17**~~ **TRANSFORMS in rebuild, shipped agents die per `2026-07-rebuild-map.md:53`.** See Amendment 2026-08-28. |
| M38 | 🗄 Dropped (2026-08-28) — ~~G-Report (`/g-report` — the maintenance channel the freeze story leans on)~~ **TRANSFORMS to GitHub issues in the freeze announcement.** See Amendment 2026-08-28. |
| M40 | 🔀 Absorbed into M52, slimmed (2026-08-28) — ~~Reference Convention (committed reference material stops being mis-gated)~~ **Slimmed 2026-08-28** — Wave 1 only (REFERENCE classifier arm + taxonomy row + g-init line + tests). Waves 2–3 dropped, archived in `g-docs/archive/roadmap-dropped-2026-08-28.md`. See Amendment 2026-08-28. |
| M43 | 🗄 Dropped (2026-08-28) — ~~Operator Controls (`/g-settings` + inspection cadence)~~ **Dotfile layer TRANSFORMS/DIES in rebuild per `2026-07-rebuild-map.md:64`.** See Amendment 2026-08-28. |
| M49 | 🗄 Dropped (2026-08-28) — ~~Devil's-Advocate Agent (internal adversarial pattern review) — **folded in 2026-08-14**~~ **New agent in dying review-orchestrator layer per `2026-07-rebuild-map.md:26`.** See Amendment 2026-08-28. |
| M41 | 🗄 Dropped (2026-08-28) — ~~Release Machinery + README Currency — **cuts the 2.5.0 release, sequenced last**~~ **Replaced by M52 hand-cut release per §D.** See Amendment 2026-08-28. |
| M52 | v2.5 Minimal Freeze — hand-cut release containing M47 + M48 + slimmed M51/M40. Four implementation sessions as planned — *(2026-08-30 amendment: audit cycles F1–F3 inserted before Session D; release when F3's gate closes)* (plan: `g-docs/plans/v25-minimal-freeze.md`). |
| shipped | Check 24 injection detector; `/g-init` lib-install fix (`ec9bf8a`) |

All in-2.5 milestones ship cumulatively in **v2.5.0**, the single final G-Forge release label — no v2.6+ ever ships from this repo. Intermediate patch/minor releases below that label (v2.4.x) may cut at milestone-family closes — **amended 2026-08-22**, see "Amendment — 2026-08-22" above.

**Everything else forks to G-Proof:** M25, M26, M29–M37, M39, M42, M44, plus both backlog candidates (multiplayer orchestration, unified provenance). Their entries move to `g-docs/g-proof-roadmap.md` — ~~a gitignored, local-only file by explicit developer choice (2026-08-10), carried to the fork by hand~~ *(superseded 2026-08-21: the file was un-ignored and committed, `45e21ad`, gaps A9 developer directive — `.gitignore:67`. It forks with the repo; no hand-carry step, no fork-checklist item.)* The reason for the reversal was the risk this ADR itself records below: a 42KB single-copy planning doc is a known-fatal loss pattern, and this repo has lost a gitignored planning file once before (CLAUDE.md, regenerated 2026-07-10).

Related calls made the same day:

- ~~**M41 stays in 2.5** (developer, over the session's recommendation to hand-cut the release) — the freeze release itself goes through the gated pipeline M41 builds.~~ *(Superseded 2026-08-29 — amendment 4 reverses this: M41 is dropped and archived, and M52 hand-cuts v2.5.0. The session's original recommendation is what was adopted, on the new evidence that v2.4.1's hand-cut shipped.)*
- **README carries the full 2.5 comms treatment** — published 2026-08-10, ahead of the release, by an explicit developer **override** of the plan's §4 publish-at-release rule (the override and its two sanctioned copy edits are recorded in `communication-plan-2.5.md`'s header). Remaining surfaces (CHANGELOG 2.5.0 heading, announcement) still publish at release.
- **M38's delivery reconciliation stays open** (§7 item 2: "hands you a file" vs via-git) — decided at M38 plan time, not here. The keyline report arriving as a hand-written file is standing evidence the file path works.

## Constraints That Drove This Decision

- ~~**The approved copy is a commitment.**~~ *(Superseded 2026-08-28 — dissolved by name in amendment 4: the sole adopter accepted a thinner 2.5, and no outside reader is bound by the hedged "What's coming in 2.5" copy. The copy follows reality, not the other way round.)* `communication-plan-2.5.md`'s §3c copy (developer-approved 2026-07-28, changes are a developer decision) promised seven concrete items; the rewrite is done, `communication-plan-2.5.md` §3c.
- ~~**§3a structurally depends on `/g-report`.**~~ *(Superseded 2026-08-28 — amendment 4 drops M38 and re-routes the report channel to GitHub issues in §3a itself; the announcement's load-bearing sentence now names a channel that exists today.)*
- **ADR-010 is fixed ground:** v2.5 is the last release, the repo freezes, G-Proof ships 1.0 from a fork, no G-Forge 3.0. This decision chooses 2.5's *contents*, never its *identity*.
- **The roadmap contradicted itself:** "only M41" in three places vs. four §3c items stamped v2.6.0–v2.15.0 — an unsettled scope was blocking the comms plan, the audit, and all sequencing.

## Alternatives Considered

1. **Minimal freeze — M41 + M47 + M48 only, fork everything else.** Fastest path to the fork. ~~**Rejected (developer):** cuts four of §3c's seven bullets *and* breaks §3a's `/g-report` sentence — the biggest copy rewrite and the weakest freeze story.~~ *(Superseded 2026-08-28 — **reopened and chosen** in kind by amendment 4, on the evidence that the copy binds nobody and the rebuild map marks the cut components DIES/TRANSFORMS. Both stated costs were accepted: the §3c rewrite is done, and §3a's channel became GitHub issues. The chosen scope drops M41 as well and adds slimmed M51/M40 items — see "Amendment — 2026-08-28: minimal freeze" above.)*
2. **Middle scope — M41 + M38 + M47 + M48** (the session's recommendation: keep what the freeze story needs, fork M45/M43/M40). **Rejected (developer):** still cuts three announced items; the copy would be rewritten around a thinner release instead of the release honoring the copy.
3. **Hand-cut the release, fork M41 too** (session's pushback: machinery built for a single use). ~~**Rejected (developer):** the freeze release itself goes through the gated pipeline — the last release is exactly the one that should not be hand-walked.~~ *(Superseded 2026-08-28 — **adopted** by amendment 4, which names the new evidence the 2026-08-10 rejection did not have: v2.4.1 was hand-cut per G-RULES §D on 2026-08-24 and shipped. M41 is dropped; M52 hand-cuts v2.5.0.)*
4. **Full copy scope** — ~~**chosen.**~~ *(Superseded 2026-08-28 — **reversed** by amendment 4; alternative 1 is chosen in its place. See above.)* The release would ship what the announcement says, verbatim.

## Consequences

**Easier:**
- ~~The announcement stays true word-for-word — no copy renegotiation, no §3c rewrite, no weakened freeze story.~~ *(Superseded 2026-08-28 — the §3c rewrite is exactly what amendment 4 does; the copy now follows the release rather than binding it.)*
- ~~Every 2.5 milestone now carries the same version stamp and one recorded build order — planning and release sequencing read from one place.~~ *(Superseded 2026-08-28 — the nine-milestone build order is retired; M52's four sessions replace it, `g-docs/plans/v25-minimal-freeze.md`.)*
- `communication-plan-2.5.md` §7 blocking findings 1 and 3 close with the re-scope pass this ADR anchors.

**Harder:**
- ~~The freeze date moves out: nine milestones ship before the fork, not one. Accepted knowingly.~~ *(Superseded 2026-08-28 — reversed: the freeze date moves **in**. M52 cuts v2.5.0 on Sunday 2026-08-30.)*
- ~~The G-Proof start date moves with it — every fork-bound milestone waits behind the full 2.5 arc.~~ *(Superseded 2026-08-28 — the fork is no longer gated behind the full arc; the dropped entries become G-Proof candidates decided at R0, `archive/roadmap-dropped-2026-08-28.md`.)*

**Follow-up work:**
- Finding 2 (M38 delivery: "hands you a file" vs via-git, + hook-trigger/report-menu scope) is carried to M38's plan gate.
- "Only M41" retired in the committed record — `ROADMAP.md` Version Plan, `project_brief.md`, `M41.md`, `communication-plan-2.5.md` (header + §7, swept 2026-08-10 after doc-review caught two survivors). One historical occurrence survives **by design** inside the M44 ADR-010 stamp in `g-proof-roadmap.md` (committed since 2026-08-21) — struck through and stamped superseded rather than rewritten, because that stamp is ADR-010's dated text.
- The Version Plan's v2.6.0–v2.15.0 tail is deleted, not re-stamped — those numbers never ship; versioning restarts as G-Proof 1.0 per ADR-010 (unamended).

**Risks:**
- ~~*Scope fatigue* — nine milestones under one version invites mid-arc re-cuts; any narrowing after release copy is published falsifies the announcement (this is the one-way edge of this two-way door).~~ *(Superseded 2026-08-28 — **this risk fired, and the door held.** The mid-arc re-cut happened; the narrowing did not falsify the announcement, because the published copy was hedged future-tense and bound no reader. Amendment 4 rewrote the copy rather than the release. The one-way edge was never actually crossed.)*
- ~~*The gitignored `g-proof-roadmap.md` gets lost* — machine-local by developer choice; mitigated by the committed pointer in `ROADMAP.md`'s Backlog, the git-history recovery path, and the named fork-checklist item.~~ *(Closed 2026-08-21 — the mitigations were judged too thin for a 42KB single-copy doc, so the file was un-ignored and committed instead, `45e21ad`, gaps A9 developer directive; `.gitignore:67`. The risk no longer exists.)*

## Assumptions That Held (verify at 2.5 close)

- ~~The §3c copy's per-item claims still match what each milestone actually shipped (§7 "re-verify at publish time" list — especially "same verdict, a fraction of the cost" — originally pending M45's A/B; M45 folded into M51 (2026-08-20), so the obligation transfers to M51's panel wiring, which must demonstrate verdict-equivalence (or record the trade) before this claim publishes, and "recalibrated" requiring genuine ground-truth calibration in M47).~~ **[2026-08-28 amendment] Assumption reversed.** The sole adopter accepted the thinner freeze; the §3c copy is rewritten before the 2026-08-30 cut to describe what M47 + M48 + slimmed M51/M40 ship, not the full nine-milestone set. Milestones marked DIES/TRANSFORMS in the rebuild map are removed from the announcement before publication.
- ~~M41 stays in 2.5 as the release-cutting milestone (developer call 2026-08-10).~~ **[2026-08-28 amendment] Assumption reversed.** M41 is dropped; M52 hand-cut release replaces it. The hand-walk machinery works (v2.4.1 cut 2026-08-24), which it did not when alternative 3 was rejected 2026-08-10.
- ~~`g-docs/g-proof-roadmap.md` still exists on the working machine at fork time and is on the fork checklist.~~ *(Retired 2026-08-21 — the file is committed, `45e21ad`, `.gitignore:67`; it forks with the repo and the fork-checklist item is gone. Re-stamped 2026-08-29 at the r4 doc gate.)*
